# Mindo 后台管理与付费系统 · 现状总览

本文档整理付费系统从架构设计到多轮排查修复的完整现状，供后续对照参考。技术细节以 `Mindo-支付系统.md` 为准，本文档是面向你自己的总览摘要。

---

## 一、系统架构

`packages/payments` 是共享逻辑包，不直接连接具体 Supabase 实例，通过"账本适配器"接收数据库连接，供 `apps/web`（目前唯一的 app，尚未分家）调用。

用户拥有三类资产，账户级别统一管理，互不干扰：

1. **虚拟币**——买断制服务用，永久不贬值、不可转让、不可提现。
2. **VIP 会员**——订阅制服务用，只看 `users.vip_expires_at` 是否过期，`vip_tier` 字段已废弃、全项目零引用。
3. **兑换券**（原"服务覆盖凭证"）——锁定单一具体服务的一次性资格，可以是全免/百分比折扣/固定金额抵扣，用户自己选择用不用、用哪张。

现阶段唯一入账渠道是兑换码，Stripe/微信支付/支付宝暂未接入。

---

## 二、数据库表清单

| 表名 | 用途 |
|---|---|
| `wallets` | 虚拟币余额，一人一行 |
| `wallet_transactions` | 虚拟币流水，每笔加减留痕 |
| `service_prices` | 各服务消耗多少虚拟币，后台可配 |
| `wallet_topup_tiers` / `wallet_topup_tier_prices` | 充值套餐（档位+多货币定价），替代已废弃的 `wallet_topup_rates` |
| `vip_transactions` | VIP变动流水（状态本身在 `users.vip_expires_at`） |
| `service_coverage_vouchers` | 兑换券，含 `service_type`/`coverage_type`/`coverage_value`/`remaining_uses`/`issuer_label` |
| `redemption_code_batches` / `redemption_codes` | 兑换码批次与具体码 |
| `system_alerts` | 需要管理员介入的系统警报（API密钥失效/数据缺失/内容政策连续拦截） |
| `bazi_readings` | 报告记录，含 `ai_reading_status`/`deleted_at`/`retry_count`/`content_policy_retry_count`/`alert_status`/`charge_type`/`charge_wallet_amount`/`charge_voucher_id`/`charge_refunded_at`/`first_attempt_at`/`last_attempt_at` |
| `admin` | 后台管理人员账号，独立于 `public.users`，详见第三节 |

已删除：`products`、`purchases`、`subscriptions`（原 Lemon Squeezy 体系，无真实数据，直接废弃）。

`bazi_readings` 的 RLS 已收紧为只保留 `SELECT` 策略，所有写入必须走后端 service role 接口，堵死了"用户绕过接口直接写入表"的漏洞。

---

## 三、后台管理面板（`/admin`）

### 3.1 权限与账号体系（2026-08-14 更新，替代原 ADMIN_EMAILS 白名单方案）

后台管理人员账号使用独立的 `public.admin` 表，与普通用户账号（`public.users`）彻底分离，不共用登录系统，**服务全部后台功能，不止某一个模块**：

- `public.admin`：`id` 对应 `auth.users.id`，密码存储/验证复用 Supabase Auth（不自行实现加密逻辑），字段通用（邮箱/显示名/创建时间/最近登录），供任何后台模块直接复用
- 登录入口 `/admin/login`：纯账号+密码，无验证码、无第三方登录、不开放自助注册
- 账号创建：`apps/web/scripts/create-admin-account.mjs`（`node --env-file=.env.local scripts/create-admin-account.mjs <邮箱> <密码>`）
- 全部后台页面收拢在路由分组 `apps/web/src/app/admin/(protected)/` 下（分组不影响实际URL），统一由 `admin/(protected)/layout.tsx` 用 `requireStaffAccount()` 做鉴权闸门；`/admin/login` 单独在这个分组之外，避免"未登录→跳登录页→登录页也被拦→死循环"
- 原 `ADMIN_EMAILS` 环境变量白名单机制已完全移除（`lib/admin/requireAdmin.ts` 已删除），不再需要在本地 `.env.local` 和 Vercel 生产环境分别维护这份名单

**内容库模块额外一层权限**：内容库（`/admin/codex`）在通过 `requireStaffAccount()` 之后，还会再查 `codex_admin_scopes` 表判断可管理的分类范围（无记录=不限范围，有记录=仅限指定分类及其子分类）。**这层分类颗粒度权限目前只服务内容库**，本节列出的其余后台页面（兑换码批次、直接发放等）没有类似的细分权限，只要是 `admin` 表里的账号即可访问全部功能，暂无按模块/按操作细分权限的计划。

### 3.2 页面清单

| 路径 | 功能 |
|---|---|
| `/admin` 兑换码批次 | 生成批次（类型优先选择：兑换券/虚拟币/VIP时长，按类型动态展示对应字段）、批次列表、CSV导出 |
| `/admin` 直接发放 | 按 handle 搜索（管理员场景下能搜到自己）、直接发虚拟币/VIP天数/指定服务兑换券 |
| `/admin/prices` | 价目表，service_type 下拉选择（读 assessments.ts，不显示原始字符串） |
| `/admin/rates`（充值套餐） | 档位+多货币定价管理，替代原"充值汇率" |
| `/admin/alerts` | 系统警报列表，可标记已处理 |
| `/admin/readings/[id]` | 单条报告诊断页 |
| `/admin/codex` 内容库 | 词条/归类的自建管理后台，详见 `Mindo-内容库.md` 第七节 |
| 用户账本查询 | 输入邮箱/handle查询余额/VIP/持有兑换券，只读 |

---

## 四、报告生成流程（现状）

1. 用户打开档案报告页（`?snapshotId=`），免费查看价格 + 兑换券卡片（有几张显示几张，样式与"我的资产"页复用同一组件）。
2. 点击生成 → 弹出确认弹窗（同一弹窗切换内容：确认→加载中→已开始生成/失败提示）。
3. 确认后调用 `/api/ai/reading`：服务端校验该档案没有未软删除的现存报告 → 按选择扣虚拟币或核销兑换券 → 创建 `bazi_readings` 记录 → 触发 `generate-phase1` Edge Function。
4. 弹窗关闭后，前端不刷新页面，直接更新本地状态 + `window.history.replaceState` 同步地址栏为 `?readingId=`，立即展示骨架屏（骨架屏顶部悬浮提示"预计3-15分钟，可以先离开"）。
5. 五段生成（phase1→theme1→theme2→theme3→theme4）依状态接力，`ai_reading_status` 全程驱动页面渲染，完成后状态变为 `done`。

失败分类与重试（`reading-recovery` 定时任务，Supabase pg_cron 每2分钟触发一次，因 Vercel 免费版 cron 只支持每日一次而改用此方案）：

- **技术性失败**（超时/限流/解析失败）：计入 `retry_count`，达到10次或超过45分钟自动判定失败，触发退款/归还兑换券 + 软删除报告记录（档案立即恢复可重新生成）。
- **内容政策拦截**：独立计数 `content_policy_retry_count`，达到5次写入 `system_alerts`，继续重试不放弃。
- **API密钥失效/数据缺失**：立即写入 `system_alerts`，不计入重试次数，持续重试直至管理员处理。

删除报告为软删除（`deleted_at`），付费凭证数据不物理清除；只有状态为"已完成"的报告允许用户手动删除，生成中/卡住状态禁止删除（此前的漏洞：删除按钮曾绕开退款逻辑直接清除记录，已修复）。

---

## 五、多轮排查修复记录（时间线摘要）

1. **RLS兜底**：`wallets`/`wallet_transactions`/`service_coverage_vouchers` 加了"只能读自己"的行级安全规则，作为后端接口权限判断的兜底，不影响后端 service role 操作。
2. **原子操作**：扣款/退款/VIP续期/凭证核销/兑换码核销全部通过 Postgres 数据库函数（`deduct_wallet`/`credit_wallet`/`extend_vip`/`consume_voucher`/`redeem_code`）完成，避免"先查后改"之间的竞态窗口。
3. **扣费时机修正**：从"点卡片即扣款"改为"点卡片免费进入，点确认按钮才扣款"。
4. **档案隔离**：报告生成状态严格按 `profile_id` + `deleted_at IS NULL` 隔离，服务端补上此前完全缺失的"该档案是否已有报告"校验（此前只靠前端隐藏按钮，存在重复扣费漏洞）。
5. **Edge Function 迁移入库**：五个 Edge Function 此前只存在于 Supabase 后台、从未版本控制，本轮迁入 `supabase/functions/`。
6. **超时保护**：Gemini 调用从无超时保护（导致进程被平台静默强杀）改为显式超时，数值随排查几经调整，最终定为100秒（Supabase免费版 `Maximum Duration` 上限150秒）。
7. **重试静默失败根因**：`reading-recovery` 重试 `generate-phase1` 时请求体缺少 `snapshotId` 字段，被参数校验直接拒绝（400），但调用方不检查响应状态码，导致 cron 长期"自认为在重试"、实际从未真正触发过 Gemini——已修复（去除该死参数的校验要求 + `retriggerReading` 补上非2xx状态的日志)。
8. **URL同步缺失**：弹窗流程改造后，生成中地址栏未同步更新为 `?readingId=`，导致骨架屏阶段刷新页面会跳回付费页——已用 `window.history.replaceState` 修复。
9. **翻译键缺失**：`bazi.reading.priceLabel` 只写入英文文件、中/法文缺失，已补齐；顺带清理了兑换券说明文案的重复定义。
10. **后台账号体系重构**：`ADMIN_EMAILS` 环境变量白名单机制替换为独立的 `public.admin` 表账号体系，详见第三节3.1。

---

## 六、本轮代码健康度审查发现（待处理）

**已确认可直接清理：**
- `snapshotId` 死参数：`generate-phase1` 已不校验，但 `/api/ai/reading/route.ts` 调用时仍在传递，需同步删除。
- `resolvedTheme`：`BaziReadingView.tsx` 声明但从未使用的变量。
- `components/modules/bazi/ReadingCard.tsx`：孤儿文件，全项目零引用，内部仍调用已废弃的 Lemon Squeezy 接口，建议删除。
- `refundCharge()` 封装：`/api/ai/reading/route.ts` 和 `reading-recovery/route.ts` 各自独立实现了一份退款逻辑，判断结构不一致，属于"两处维护同一业务规则、容易改漏一处"的隐患，需要在 `packages/payments` 里统一封装成一个函数，两处改为调用它。
- 日志精简：`generate-phase1` 排查期间加的埋点日志中，"即将调用Gemini/dataSheet长度""Gemini调用返回/文本长度""last_attempt_at写入成功/失败"这三类建议保留为长期可观测性指标；"开始执行""准备写入...""查询返回：hasDraft=..."这类过程旁白建议精简/删除。
- 待清理的历史 `CLAUDE.md` 待办：`vip_tier` 零引用、`app/api/dashboard/profile/page.tsx` 已不存在，两条可以划掉。

**需要你决定，尚未定案：**
1. **theme1~4 要不要也补上"Gemini调用返回，文本长度=X"这一行日志？** 这次bug已确认只影响phase1，theme1~4没有同样风险的证据，但这行日志的价值是通用的健康度指标，成本很低。
2. **兑换券卡片化之后，`issuer_label`（发行方标签）在用户界面完全不再显示，要不要补回卡片上？** 需要你确认这个信息对用户是否重要——如果只是内部记账用途，不需要补；如果原意是让用户知道"这张券是谁发的"（B2B场景），这是需要补的功能回归。

---

## 七、尚未开始的功能（讨论过但明确推迟）

- **站内消息中心**：报告完成通知、促销/赠送提醒的统一承载。当前"生成完成后回来看就有了"这类文案未承诺主动通知，是有意为之，避免做出兑现不了的承诺，记入技术债，等消息中心做了再回来改文案。
- **邮件通知**：`notifyAdminAlert()` 函数目前只打日志，未接入真实邮件服务（计划用 Resend，需要先有正式域名做发件人验证）。以后除了系统警报通知，还要承担支付正式接入后的发票发送功能。
- **Stripe/微信支付/支付宝**：尚未接入，充值套餐（`wallet_topup_tiers`）表结构已经就绪，供以后直接复用。
- **分类/模块级细分权限**：目前 `admin` 表账号要么是"某个人能不能进后台"这一刀切判断，只有内容库模块单独做了"能管哪些分类"这层细分（`codex_admin_scopes`）。其余模块（价目表、直接发放等）暂无按操作细分权限的计划，等实际需要时再设计，不提前搭建通用权限框架。

**已完成，从本节移除**：原"统一后台"事项（内容库后台与支付后台此前分属两套系统，`/admin` 走 ADMIN_EMAILS、内容库走 Keystatic 本地模式）已于账号体系重构中一并解决——内容库的编辑后台已改为自建（不再是 Keystatke），且与支付后台等全部收拢进同一套 `admin` 账号体系和同一层路由鉴权之下，不存在"暂不方便切换"的问题。