# Mindo 内容库（Codex）架构决策文档

## 一、定位与目的

内容库是SEO/GEO/ASO策略的核心载体，同时服务三个对象：

- **搜索引擎**：关键词密度（决定语料库=最小单位）
- **AI（GEO）**：结构化（决定外壳=版式）
- **人类读者**：认知方式（决定内容如何在外壳中排列）

差异化定位：欧美市场目前没有对应的高质量内容库，中文市场现有内容普遍陈旧，Mindo的内容新颖度和系统性本身就是竞争优势，也是对冲"内容局部重叠"这类SEO风险的最有效手段（见第四节）。

---

## 二、颗粒度政策

**默认原则：一个传统命理/玄学概念 = 一个词条，不做子主题拆分。**

例如"正官"是一个词条，"正官判定""正官代表的心理机制""正官宫位"等，都是"正官"这一篇词条内部的小节，不单独开设子词条。这样处理是为了保持概念一体性，避免过度拆分破坏阅读的完整性，同时控制单人产出内容库的工作量。

例外处理方式：若某个子主题未来被证实有独立的高搜索量/高价值（需要真实SEO数据支撑），届时可以针对性拆分，不需要提前预判。

---

## 三、词条外壳模板（写作规范）——⚠️ 未确认，仅为早期参考草稿

**状态说明：本节内容来自项目更早期讨论中的一份草案，用户已明确指出"这个环节应该在准备动笔写内容之前才讨论"，此后未被重新讨论或正式确认。本节不构成架构决议，仅作为未来讨论外壳规范时的参考起点，实际规范需重新与用户逐条确认后才能生效。**

以下为草案原文，供参考：

适用于每一篇独立词条，固定结构（内容具体撰写细节留待写手/正式施工阶段落实，此处仅定骨架）：

1. **第一句话**：交代该词条的直接前置知识（仅直接前置，不追溯全部血统），附带超链接
2. **第二句话**：阐明该概念的用途
3. **正文开头**：给出具体可感知的意象锚点（概念隐喻，如"甲木=大树"）
4. **正文内容**：结构化展开
5. **对比型组织者**（如适用）：天然成对的概念（如正官/七杀）可做对照区块
6. **双重编码**：每个词条配一个极简视觉符号，图文邻近摆放

**写作纪律**：同一篇文章中，同一词条只在首次出现时加链接（首次原则）；只有存在独立词条的概念才加链接；格式规则零例外。

---

## 四、SEO风险的真实边界

- 搜索引擎判定的"重复内容"，指向大段文字近乎逐字相同的机械化复制，不是"两个页面涉及同一主题"
- Google对内容重叠的常规处理方式是"过滤"（多个高度相似版本中只挑一个代表出现在搜索结果中），不是对整个页面/网站做惩罚性降权
- 真正触发处罚的是大规模、机械化、以操纵排名为目的的复制行为
- 页面整体质量与独特性的权重，远大于其中一小段内容重叠带来的负面信号

---

## 五、URL变更纪律

以"该内容是否拥有独立URL、已被搜索引擎索引"作为判断依据：是，则挪动/改名时需要设置301重定向；否，则内部结构调整无需任何面向搜索引擎的额外操作。

**现行架构下的说明**：当前词条的网址由 `codex_entries.slug` 决定，`slug` 是词条创建时就定下来、且和词条永久编号绑定的标识，不随归类调整而改变（详见第七节7.2）。归类本身的移动/改名/删除不会改变任何词条的网址，因此这套架构下"移动内容"这个动作本身通常**不会**触发需要重定向的情况；只有主动修改某个词条的 `slug` 值本身，才需要考虑给旧路径配置重定向（当前后台尚未开发自动重定向配置功能，需人工评估是否值得为个别改名操作添加）。

---

## 六、已废弃方案（历史参考）

> 以下内容记录内容库从"文件系统 + Fumadocs + Keystatic"演变到"数据库驱动 + 自建后台"过程中被推翻的历次方案，**代码已全部移除，不代表当前架构**（当前架构见第七节）。保留在此仅供理解决策脉络、避免未来重蹈同一类结构性错误。

### 6.1 技术选型：Fumadocs（已废弃）

曾采用 `fumadocs-core` + `fumadocs-mdx` 作为内容库底层框架，理由是无头架构（数据层与UI层解耦）、内置搜索索引、原生支持Next.js App Router、MIT许可证无供应商锁定。

### 6.2 文件架构：概念优先 + 文明分层（已废弃）

曾采用"一个词条一个文件夹，内含全部语言版本文件"的存储方式，归类靠嵌套文件夹路径表达（如 `codex/china/bazi/shishen/zhengguan/`），并引入"文明分层"（`china/`、`india/`、`europe/`等）与"紫微斗数原则"（词条归属跟随产品建设进度而非历史起源）两条组织原则。

**废弃原因（核心教训）**：文件夹路径同时承担"网址结构"和"词条身份"两个职责，这是此前六轮排查（树状导航做不到、新建路径靠手打容易出错、归类调整等同于移动物理文件、9个语言文件容易在挪动中失散）反复撞墙的结构性根源。当前架构（第七节）通过给词条分配独立于物理位置的永久编号，从根本上解耦了这两者——这是整个重建过程中最重要的一条经验，未来任何类似的内容组织设计都应该优先避免"用存储位置表达身份/分类关系"这种耦合。

### 6.3 共享片段机制（已废弃）

曾设计"同一段内容可以被多个词条引用、只有一个归属方存放正文"的机制（如"甲木作为日主"这段文字同时被"日主"总览页和"甲"词条引用），用具名`export`的方式在MDX文件间共享文本片段。这一机制随文件存储方案一起废弃，当前架构下如需类似效果，可以用第七节的站内链接（Xref）配合正文自行组织，不再有专门的片段复用技术机制。

### 6.4 技术地基施工踩坑记录（工程经验，仍有参考价值）

以下是 Fumadocs + Keystatic 时期积累的具体技术教训，虽然对应的工具已不再使用，但反映的问题模式具有一般性参考价值：

- **i18n目录约定与框架内置loader不匹配时**，可以在喂给框架前对虚拟路径做一层内存中的重映射，不强求磁盘目录结构去将就框架的默认约定
- **多个rehype/remark插件之间可能存在写死的执行顺序**，不受配置数组顺序影响，遇到"配置了选项但不生效"时，要考虑插件本身是否在更底层已经写死了处理逻辑
- **某些框架的宏（macro）只能在特定构建流程（如webpack/turbopack）里被正确解析**，脱离该流程用独立脚本直接导入会报错——独立校验类脚本如果依赖这类宏，改为实现成一个由同一构建流程启动的API路由更稳妥
- **改动构建配置后，如果测试没有反应**，优先怀疑构建工具自身的缓存/生成目录（如 `.next`、`.source`）没有被正确清理，而不是先怀疑配置逻辑本身写错——完整删除这些目录、干净重启，比反复相信"热重载已经生效"更可靠
- **服务器端和浏览器端共用同一个配置/工具文件时**，一旦要往其中添加真正需要浏览器端hook（`useState`/`useEffect`等）的交互逻辑，必须把这部分拆成独立文件单独标记为客户端组件（如 `'use client'`），不能图省事给整个共享文件加此标记——否则服务器端对该文件的其他导出（哪怕只是普通函数调用）也会被框架错误地当作"客户端专属"而拒绝执行。这条经验具有项目通用性，不止内容库模块适用

---

## 七、当前架构（数据库驱动，2026-08-14 最终状态）

内容以 Supabase（PostgreSQL）为存储，编辑工具完全自建（不依赖任何第三方CMS）。核心设计原则：**词条身份、语言内容、归类三者相互独立，通过外键关联，不通过物理路径耦合**。`apps/web-cn` 是独立仓库，本次改造未涉及，那边数据库/部署方案待中国团队一侧单独设计。

### 7.1 数据模型

**`codex_entries`（词条本体）**

| 字段 | 说明 |
|---|---|
| `id` | 永久编号（uuid），创建后终身不变 |
| `category_id` | 归属的归类（外键，**可为空**——留空代表这是一篇不归属任何分类的顶层词条，跟顶层归类是同一种"无父级节点"的性质）|
| `slug` | 网址最后一段，**全局唯一，整个词条只有一份，九种语言共用**（不按语言/发布状态区分）|
| `created_at` | |
| `published_at` | 首次正式发布时间，为未来定时发布/blog复用预留，当前未实现"定时"本身 |

**`codex_entry_translations`（每语言内容）**：`entry_id` + `locale`（zh/zh-Hant/en/fr/es/ja/ko/it/de）唯一，含 `title`、`body`（Tiptap原生JSON文档）、`status`（draft/published）、`updated_at`。**不含slug字段**（按语言各自独立存放已废弃，统一至词条本体）。

**`codex_categories`（树状归类）**：`id`、`parent_id`（自引用，顶层为空）、`home_entry_id`（该归类的"首页词条"）。删除归类时，若其下仍有子内容，自动挪至上级归类。

**`codex_category_translations`**：`category_id` + `locale` 唯一，含 `name`、`slug`（归类的slug继续按语言独立，只用于面包屑展示，不参与词条定位）。

**`codex_citations`（引用/注释）**：`entry_translation_id` + `order_index` 唯一，含 `title`、`url`。编辑器内文档状态的持久化镜像，前台渲染直接读取词条正文本身生成角标，不依赖此表。

站内链接（Xref）不建表，以正文内的行内标记（mark）形式存在，渲染时实时查询目标词条当前标题。

### 7.2 渲染与路由

访问网址时，**只取最后一段作为slug**，配合当前语言，查询 `codex_entries.slug` 直接定位词条，不解析中间路径段。有归类的词条，面包屑由 `category_id` 递归查询 `parent_id` 生成；顶层词条（`category_id` 为空）没有面包屑。**归类的移动、改名、删除都不影响任何词条的定位与已有链接的有效性**——这是废弃文件夹路径方案（第六节）后换来的核心收益。

若最后一段未匹配到任何词条，回退按完整路径逐段匹配归类树；命中且该归类设置了 `home_entry_id`，渲染对应词条作为该归类首页。

### 7.3 账号与权限

后台管理人员账号与普通用户账号（`public.users`）**彻底分离**，且**服务于全部后台功能**（不止内容库），详细设计见 `后台.md` 第三节。内容库额外一层：`codex_admin_scopes` 表控制可管理的分类范围（无记录=超级管理员，有记录=仅限对应分类及子孙分类）。**这层分类颗粒度权限目前只服务内容库场景**，且实际尚未真正投入使用（目前唯一账号是超级管理员，不受限）。

### 7.4 编辑工具（自建后台）

路由 `/admin/codex/entries`，登录后落地页。归类树与词条列表合并为单一联动工作台。

**结构管理**：

- 左侧类目树同时展示归类与词条节点（图标区分），可展开收起
- **顶层词条**（不归属任何分类）与顶层归类（如 `china`、`psychology`）平级显示在树的最上层，不设专门的"无分类词条"分区——顶层词条和顶层归类是同一种"无父级节点"，复用同一套渲染逻辑
- **排序规则**：任意一层级内，词条固定排在归类上面（硬性分组，不参与整体排序）；组内按各自的路径标识（slug）字母顺序排列。"全部词条"这个摊平视图里，排序依据是词条的**完整路径**字母顺序（如 `china/bazi/tiangan/jia`），保证同一分类下的词条自然聚在一起，不被打散
- **"全部词条"列表**：每个词条后面显示它的完整分类路径（中文名以"/"连接，如"中国/八字/天干"），顶层词条不显示任何归属提示
- 右键弹出上下文菜单（重命名/移动/创建子类/创建词条/删除）；"移动"支持选择顶层作为目标（词条和归类都可以移动到顶层），移动弹窗内的目标树默认只展开当前节点所在的那条路径，不会一次性全部展开
- **创建**：创建归类与创建词条共用同一个弹窗组件，三行结构固定为"位置→路径标识→名字"（创建归类时叫"类名"，创建词条时叫"词条名"），位置选择器是树状选择器（不可手动输入），两者都包含"(顶层)"选项，默认值是当前所在位置，弹窗内位置选择器保持展开供随意挑选（与"移动"弹窗默认只展开当前路径的行为不同）
- 名字/路径标识输入框使用占位符文字（浅灰色提示，输入后消失），不用固定文字标签
- 支持按当前选中分类范围过滤的搜索框；深浅色主题复用前台已有系统，切换入口在最顶层导航栏

**正文编辑器**（Tiptap，JSON文档存储）：

- 工具栏固定顺序：标题（H1/H2/H3+正文）→ 加粗 → 斜体 → 下划线 → 文字颜色 → 文字背景色（复用片语模块同款扩展与色值）→ 插入注释 → 站内链接 → 插入图片/视频
- 不支持列表、引用块、外部超链接；彻底关闭Markdown快捷输入（编辑器级总开关，覆盖全部格式）
- **插入注释**：独立于原文的节点，点击直接生成自动编号角标，无弹窗；内容在编辑器下方常驻的注释列表中填写/编辑，与正文角标按文档出现顺序自动配对编号
- **站内链接**：先选中文字→点击按钮→弹出搜索框（自动带入选中文字作为默认搜索词）→选定目标→包裹住原选中文字；取消不留痕迹
- **媒体块**：先弹窗收集完整数据（链接+尺寸预设+对齐方式）再插入节点，不存在"先插入空节点再补全"的中间态

### 7.5 SEO / GEO

`sitemap.xml`、`llms.txt`（每语言索引）、`llms-full.txt`（每语言全文）均基于数据库查询、仅含 `status='published'` 内容实时生成。站内搜索目前基于标题/slug关键词匹配，暂不支持正文全文检索。

### 7.6 部署

无需独立常驻服务器，前台渲染与后台管理工具均运行于现有Vercel部署，数据库为Supabase。

### 7.7 内容骨架现状

八字（五行/十天干/十二地支/十神，37篇）+ 大五人格（五维度+三十子维度，35篇），共72篇词条，全部为占位内容（`status=draft`），尚未正式撰写。

### 7.8 尚待完成事项

| 事项 | 状态 |
|---|---|
| 内容库根首页视觉 | 目前仅占位标题，尚未设计 |
| 站内搜索全文检索 | 未实现，待需求明确 |
| 分类权限范围管理界面 | 未开发，暂靠手动操作数据库 |
| 中国大陆独立部署与数据同步方案 | 未设计，由中国团队一侧负责 |
| 协作者后台账号 | 未创建，机制已就绪，待实际需要时按7.3节流程创建 |
| 消歧义机制 | 不做额外技术开发，由写手在正文中手工列出撞名候选（消歧义页即普通词条）|
| 浏览器端到端人工验证 | 历轮改动多以类型检查+代码审查+接口探测方式验证，尚未做过完整的真实用户操作路径验证 |