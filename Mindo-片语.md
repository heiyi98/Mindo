# Mindo 片语模块 — 架构决策文档

## 一、模块定位

- 独立模块，与论坛无关联，不复用论坛的治理/权限体系
- 类似"一言""平行世界"：用户发布一段文字，可选择性附带个人命理/心理量化信息
- 用户可刷到其他人发布的卡片，排序由"新鲜度 + 人格相似度"共同决定
- 提供"关注"与"推荐"双栏入口（见第九节）

## 二、内容与形态

- 当前版本：纯文本 + 基础富文本（见第十六节），不含媒体附件
- 远期：支持图片、音频等媒体附件（架构需预留，见"数据分层"一节）
- 卡片渲染用普通 HTML + CSS，**不用 SVG**——参考一言等同类产品的实际做法（已查证：均为普通 div + CSS，非 SVG/非图片生成），且富文本的换行、多语言字体切换、竖排等效果，原生 HTML 天然支持，SVG 需要额外模拟，没有必要

## 三、隐私档位

四档，存在卡片主表的一个字段上：

1. 仅自己可见
2. 仅好友可见（好友 = 互相关注，即双向 follow，直接复用现有 `follows` 表判定，不新建"好友"概念表）
3. 仅粉丝可见（关注了发布者的人）
4. 所有人可见

隐私档位过滤对"推荐""关注"两个入口都必须生效，不因入口不同而放宽（例：单向关注但未构成"好友"时，关注 tab 里也不应看到对方设为"仅好友可见"的卡片）。

## 四、个人信息关联方式

- **不是**实时引用当前档案（profile）数据
- **不是**整份档案快照
- 是"发布时用户主动选择带上的字段值"本身，直接复制进卡片关联的数值记录里
- 效果：档案后续修改、重新测算、甚至档案被删除，都不影响已发布卡片显示的数值
- 这与 `bazi_readings` 的"生成那一刻钉死"原则一致，但对象更细粒度（字段级而非整份快照）

## 五、数据分层原则（极简 + 解耦 + 接口隔离）

三层分离，任何一层的扩展不影响其他层：

1. **卡片主表** `mind_cards`：只存卡片本体——发布者（`user_id`，不挂 `profile_id`，因为卡片是用户主动发布的内容，不是某个 profile 的附属物）、发布时间、隐私档位、富文本内容。新增媒体类型不改动此表核心结构。
2. **卡片附件表** `mind_card_attachments`：存卡片关联的媒体（图片/音频/未来的视频等）。MVP 阶段不建此表（当前无媒体需求），预留设计，字段大致为 `id`/`card_id`/`attachment_type`/`url`/`created_at`。
3. **卡片数值快照表** `mind_card_metrics`：存卡片发布时选择带上的量化数值。一张卡片可对应零到多条记录，每条记录标明"类型"（如 `wuxing`/`bigfive`）和"具体数值"（JSONB）。新增命理/心理维度时，是新增一种类型值，不改表结构、不影响已有维度的存储。

## 六、相似度算法

### 6.1 单因子计算逻辑

每个人格维度（五行、大五……）各自独立成一个"因子"：

- 把该维度的数值看作 N 维空间中的一个点（五行=5维，大五=5维）
- 两个用户在该维度的相似度 = 两点间的几何距离（欧氏距离或余弦相似度），换算为 0~1 分数，1 为最相似
- 五行数值直接复用 `computeWuxingAssessment`（yongshen.ts）已有的量化产出，不新建一套独立计算逻辑
- 大五复用现有五个 domain 分数
- 当前版本：命盘相似度仅用五行，不并入十神（避免一次性复杂度过高，留待后续迭代）

### 6.2 多因子合并

- 总人格相似度 = 各因子加权平均
- **权重分配原则：平分**——当前激活了几个因子（即用户双方都有数据的维度），权重就平分成几份
  - 例：只有五行数据 → 五行因子权重 100%
  - 五行 + 大五都有 → 各 50%
  - 未来新增紫微/MBTI 等，因子数量增加，权重自动重新平分，无需手动调整已有因子权重
- 任一因子若一方或双方缺失该维度数据，该因子跳过，不参与本次计算，也不拉低总分（重新归一化）

### 6.3 因子列表可扩展性

- 新增命理/心理维度（十神、紫微、MBTI、每日运势相似度等）时，只需新增一个因子，输出规则为"给定双方该维度数据，输出 0~1 相似度分数"
- 已有因子的计算逻辑、权重机制不因新增因子而改动
- 这是为长期持续增加人格维度而设计的核心架构要求（非一次性需求）

## 七、总排序公式（推荐 tab 专用）

```
总排序分数 = 60% × 人格相似度分数 + 40% × 时间新鲜度分数

```

- 时间新鲜度：随发布时间推移衰减（具体衰减曲线施工时定）
- 权重 60/40 是**可调运营参数**，非架构决策——上线后可根据实际刷 feed 观感调整，不涉及重新设计
- 相似度权重定得更高，因为"刷到相近的人"是本模块核心差异化卖点；但保留 40% 给时间，防止相似度高的用户旧卡片长期霸占排序前列，导致内容不流动
- 此公式仅用于"推荐 tab"，"关注 tab"不走此公式（见第九节）

## 八、候选人召回机制（推荐 tab 专用）

- **当前阶段**（用户量小）：直接对"最近一段时间内发布的、且当前用户有权限查看的卡片"做全量相似度计算，不做预筛选
- 已读的卡片需排除，需要一张"已读记录表 `mind_card_views`"（组合主键 `card_id`+`viewer_id`，记录 `viewed_at`），用于防止用户反复刷到同一张已看过的卡片
- **已读记录表的规模控制（MVP 范围内实现）**：该表写入量随用户活跃度线性增长，需要定期清理——候选池本身只取"时间窗口内"的卡片，一旦某张卡片超出候选池窗口，其对应的已读记录即无存在意义。MVP 阶段即实现一个每日定时任务（复用项目已有的 Vercel Hobby 每日 cron 机制，`0 0 * * `*），清理"所属卡片已不在候选池窗口内"的已读记录。表结构需支持按卡片发布时间做清理筛选（已读记录需能关联回卡片的发布时间）
- **未来扩展路径**（用户量增长后）：在全量计算之前增加一层粗筛（如按五行大类预分组），减少精算数量；此层改动不影响上面第六节相似度公式本身
- 明确不做的事：当前不引入向量数据库或机器学习召回模型（如双塔模型），这类方案依赖海量用户行为数据训练，当前数据规模不具备条件，属于过度工程

## 九、关注 / 推荐 双栏入口

- **推荐 tab**：走第七、八节的候选池+加权排序+已读去重机制
- **关注 tab**：只取"当前用户关注的人发布的卡片"，按发布时间倒序排列，不计算相似度，不需要已读记录表去重（时间线翻页天然不重复，用发布时间做分页游标即可）
- 两个 tab 共享同一张 `mind_cards` 表，只是查询逻辑不同，不需要为"关注 tab"新建任何表
- 隐私档位过滤逻辑对两个 tab 一视同仁（见第三节）
- 两个 tab 固定显示在浏览页面上方，下方是轮播主体（见第十七节）

## 十、点赞与收藏（MVP 范围内）

- **点赞**：关系表 `mind_card_likes`，`user_id`+`card_id` 组合唯一，结构参考现有 `follows` 表模式
- **收藏（不分组）**：MVP阶段曾用关系表 `mind_card_favorites`（`user_id`+`card_id` 组合唯一，结构与点赞相同）。**该表已于卡片集功能上线时废弃**——从未有真实用户数据（未正式对外发布过），因此退役时未做数据迁移，直接 `drop table`。收藏现在完全由卡片夹关联派生，见第二十八节
- **收藏夹分组 / 自己卡片的分组整理**：MVP阶段"不在范围内，留待下一期"，**下一期已完成**，见第二十八节"卡片集"
- 点赞/收藏按钮已从浏览态小卡片本体移除，改到点击展开的详情弹窗里（见第十七节）

## 十一、双服务器隔离

- 遵循平台既有的中国/国际服务器完全隔离原则，两边用户无法互相刷到对方卡片，无跨境数据共享
- 该隔离原则同样适用于第二十五节新增的翻译功能：翻译功能只接入国际服务器，中国服务器不装

## 十二、入口设计

- **仪表盘入口**：新增一张 1×2 卡片模块，遵循现有卡片系统规范（`COLS`/`ROWS`/`CARD_META`，profileId 唯一 prop 等铁律，含 SVG viewBox），点击后跳转至片语独立页面。这是唯一需要遵循 SVG/COLS/ROWS 规则的部分，因为它是仪表盘 6 列网格体系的一份子
- **片语独立页面本身**：不受仪表盘卡片系统的 SVG/COLS/ROWS 规则约束（不是网格组件），用普通 HTML/CSS，自行定义固定宽高比

## 十三、施工前必须排除的数据库隐患（独立于本模块，但会影响本模块地基）

1. ~~`profiles.is_self` 部分唯一索引~~ ——**已核实存在且定义正确**（`CREATE UNIQUE INDEX profiles_one_self_per_user ON public.profiles USING btree (user_id) WHERE (is_self = true)`），此前已排除，无需操作。
2. `bazi_snapshots` **表清理遗留字段**（`ai_reading_draft`/`ai_reading_theme1~4`/`ai_reading_status`，AI解读职责已转移至 `bazi_readings`，这几列是死字段）：
  ```sql
  alter table public.bazi_snapshots
    drop column if exists ai_reading_draft,
    drop column if exists ai_reading_theme1,
    drop column if exists ai_reading_theme2,
    drop column if exists ai_reading_theme3,
    drop column if exists ai_reading_theme4,
    drop column if exists ai_reading_status;

  ```
  执行前建议先全局搜索代码确认无遗漏读写点。
3. `follows` 表已核实唯一约束为组合约束 `UNIQUE(follower_id, following_id)`（约束名 `follows_follower_id_following_id_key`），"互相关注=好友"判定逻辑可直接放心使用，无需额外处理。

以上三项与片语本身的建表施工可以分开执行（第1、2项由用户自行在 Supabase 操作，与 Claude Code 无关），但建议在片语正式施工前处理完，避免新功能建立在有隐患的地基上。

## 十四、数据库现状（施工时须知）

- `mind_cards`/`mind_card_metrics`/`mind_card_views`/`mind_card_likes`/`mind_card_favorites` 五张表**已由用户手动在 Supabase 建完**（字段结构见本文档第五、八、十节描述），Claude Code 施工时直接对接现有表，不需要也不应该重新建表
- **RLS（行级安全策略）本次不做**：五张新表暂无 RLS 策略，隐私分级（`visibility` 字段）完全依赖后端 API 代码手动过滤，不依赖数据库层强制。参考项目已有先例（`conversation_participants`/`conversations` 同样靠 service_role 在 API 层绕过 RLS）。Claude Code 施工时，所有涉及读取 `mind_cards` 的接口，必须在查询逻辑里手动加权限过滤（判断 `visibility` + 好友/粉丝关系），不能假设数据库会自动拦截
- 已知风险（MVP 阶段可接受，非本次施工需要解决的问题）：绕过 App 直接访问数据库 API 的路径不受此层保护，待产品进入安全加固阶段另行补上 RLS，不阻塞当前施工
- 留言、收藏夹分组两项未来功能，现有表结构均可随时扩展（新增表/新增可空字段），无需现在预埋任何东西

## 十五、明确排除在本次范围之外的内容

- 图片、音频等媒体附件
- 留言（评论）功能——新子模块，需单独讨论设计（新表/回复层级/审核机制等）
- 通知系统——新子系统，需单独讨论设计（新表/推送方式/读未读状态）
- 收藏夹分组管理 / 自己卡片的分组整理——用户尚未想清楚布局（**此项已在后续"卡片集"轮次完成，见第二十八节**，本节保留作为MVP轮次的历史记录）
- 十神、紫微等相似度因子（架构已预留插槽，但暂不接入）
- 越南语/马来语/印尼语/泰语等候选语言——暂不安装到 Mindo，相关字体也不需准备
- 外语版功能命名（如英文/法文名）——暂缓，未找到合适的展示位置

## 十六、富文本编辑功能（MVP 范围内）

### 16.1 基础格式功能

- 字号：**五档**（视觉上用三个逐级放大的"A"作参照，实际可选档位由五个圆点表示，默认第 3 档）——此前定过三档，施工阶段改为五档，以三个 A 为视觉锚点、五个点为实际选择粒度
- 粗体、斜体、下划线、文字颜色：**选区层**——只对用户选中的文字生效；未选中任何文字时点击，效果作用于接下来输入的新文字（Word 式行为）
- 文字背景色（施工阶段新增，见 19.5）：同样是选区层
- **字号、字体、横排/竖排、水平对齐、垂直对齐：整卡片层**——始终整张卡片统一，不随选区变化。字体在施工过程中曾短暂尝试改为选区层（BIU式），但因为字体的核心痛点是"简体/繁体/日文的关联跳转"（见第十九节），关联跳转的对象天然是整张卡片，不是某一小段文字，最终确认维持整卡片层不变
- 水平对齐：左/中/右
- 垂直对齐：置顶/居中/置底（卡片容器有固定宽高比，文字量少时此设置决定文字块在卡片内的整体位置）
- **默认卡片样式**：横排、水平居中、垂直居中（`align:'center', valign:'center', vertical:false`）。此前默认是左对齐/置顶，施工后期改为居中居中，横排/竖版默认仍是横排

### 16.2 竖排

- 支持竖排，**书写方向与水平对齐绑定**（不是独立设置，见第二十五节）：左对齐 → `writing-mode: vertical-lr`（第一列固定左边，越写越往右长）；右对齐/居中 → `writing-mode: vertical-rl`（第一列固定右边，越写越往左长，传统竖排习惯）
- 这个绑定同时天然解决了"回车换行往哪边起新列"——writing-mode 自带的浏览器原生行为，不需要额外写换行逻辑
- **拉丁字母/数字竖排朝向**：不采用默认的"整体侧躺 90 度"（`text-orientation: mixed`，阅读需要歪头），改用 `text-orientation: upright`，每个字母/数字各自正立、按顺序往下堆叠。原因：片语竖排功能主要服务东亚用户，对拉丁字母竖排阅读习惯不敏感；数字与拉丁字母在这套系统里是同一类（国际通用文字分类标准），不能只挑数字单独处理，是整体切换
- **对所有用户开放，不分语言限制**——不因某语言用户大概率用不到而阉割功能

### 16.3 明确不做

- 项目符号列表、标题层级、表格等复杂排版功能

## 十七、浏览界面（片语独立页面主体）

### 17.1 布局

- 页面上方：关注 / 推荐 双栏切换（见第九节）
- 页面主体：横向轮播，一次只完整显示一张卡片，左右各露出半张相邻卡片，两侧边缘做渐隐效果
- 切换方式：滑动一次或点击左右露出的半张卡片，每次只前进/后退一张，不会连续划过多张

### 17.2 已读判定

- **"来到"某张卡片即视为已读**，不是"离开"时才算——包括首次打开页面看到的第一张卡片，也在渲染出来的当下就标记已读
- **推荐 tab 的"上一张"**：指本次会话内已经划过的那一张（会话内历史栈），不是重新向推荐算法请求"上一条推荐"（推荐算法本身不存在稳定的"上一条"概念）
- **关注 tab 的"上一张"**：按时间线，就是更新的那一条，语义明确，走分页游标即可

### 17.3 候选池预取

- 候选卡片划到快耗尽之前，无缝在后台预取下一批，不等到划完最后一张才去请求，避免出现"到底了卡顿"的体验
- 因为推荐 tab 的候选是一次性算好分数排好序整批返回的，相邻卡片内容已在本地，不存在临时加载空白的问题

### 17.4 卡片无限排列（生长）与浏览态呈现（新增，见第二十五节）

- 卡片允许朝一个方向无限排列：横板向下无限排列（文字打满自动换行），竖版向左或向右无限排列（取决于对齐方式，见 16.2）
- **浏览态小卡片缩略图**（本节轮播用的卡片）：固定 3:4，`overflow:hidden` 裁切超出内容；若内容触碰了无限排列（即真实内容比 3:4 框大），在"生长方向那一侧"边缘加文字渐隐遮罩（横板渐隐底部，竖版左对齐渐隐右侧、右对齐渐隐左侧）——**只渐隐文字，不渐隐卡片背景**（遮罩只加在文字层的 CSS mask-image，不影响背景层）
- **点击卡片进入详情**：打开独立弹窗（不是原地展开），详见第二十五节

## 十八、底部圆弧菜单栏

- 自定义圆弧造型（非标准矩形工具栏），风格上与项目现有的 `ValveLogo` 等自定义 SVG 组件路线一致
- 三个按钮：
  1. **左：收藏栏入口**——自己发布的卡片 + 收藏的他人卡片，具体布局待用户后续决定（不在本次范围内）
  2. **中：加号，展开发布界面**——本次范围内为纯文本+基础富文本发布（见第十六节），不含媒体附件
  3. **右：通知入口**——本次范围内仅做占位入口，功能（点赞/收藏/留言提醒）留待通知系统单独设计时实现（见第十五节）

## 十九、字体系统

### 19.1 字体家族：统一 Source 系列

- 拉丁：`Source Sans Pro`（无衬线）/ `Source Serif Pro`（衬线）/ `Source Code Pro`（等宽）
- 中日韩：`Source Han Sans`（无衬线，思源黑体）/ `Source Han Serif`（衬线，思源宋体），官方本身就是覆盖简中、繁中（台湾/香港）、日语、韩语的同一个 Pan-CJK 家族，无需为日韩单独寻找字体
- 繁体中文统一使用 **TW（台湾）子集**，不使用 HK（香港）子集——此决策已同步更新至 `CLAUDE.md`（见该文档"多语言规则"章节）
- PDF 导出功能的字体（原 `NotoSansSC/TC/JP/KR` + `NotoSans`）一并换成同一套 Source 家族，保持项目内字体血统统一。这项改动**必须配合全局搜索**确认无其他遗漏引用（项目铁律：架构级改名需全局核查零残留）
- 中日韩字体当前只提供无衬线/衬线两档，暂不做楷体/仿宋（Google Fonts 上对应候选普遍是艺术书法风格、字符覆盖有限，不符合"兼容性强、覆盖几乎所有字"的要求）；手写体候选留待用户自行在 Google Fonts 上挑选

### 19.2 字体文件来源与托管

- **实际施工采用 Fontsource 包**（`@fontsource/`* 系列 npm 包）而非手工下载 Adobe 原始 OTF 自行切分——Fontsource 是对 Google Fonts 官方目录的打包镜像，其中 `@fontsource/noto-sans-sc/tc/jp/kr` 等包的**字节内容与 Source Han Sans/Serif 完全一致**（已查证：Debian 官方软件包说明书明确写"派生自 Source Han Sans 字体家族"），只是因为 Google 当年把这套字体重新挂上"Noto"品牌对外发布，Fontsource 照搬 Google Fonts 目录，技术名因此也叫 `Noto Sans/Serif SC/TC/JP/KR`，不是另一份字体
- **技术名与显示名严格解耦**：CSS `font-family` 里用的技术名可以是 `Noto Sans SC` 等，但用户界面上看到的名字，永远走 19.3 节的"技术名 → 本地语言显示名"映射表（显示"思源黑体"等），用户不会接触到 Noto 这个字样
- 拉丁语系用 `Source Sans Pro`**（非独立版** `Noto Sans`**）**——已查证两者是完全不同设计师的作品（`Source Sans` 出自 Adobe 的 Paul Hunt，独立版 `Noto Sans` 出自 Monotype 的 Steve Matteson），且 `Source Han Sans` 内置的拉丁字母本身就是 `Source Sans` 血统（经 CJK 协调微调），用 `Source Sans Pro` 才能与 CJK 部分血统一致
- 已确认 `@fontsource/noto-sans-tc` 对应台湾繁体标准（19.1 节"TW 子集"决策的落地包）
- 直接使用 Fontsource 已切分好的 unicode-range 分片文件（原计划"用 Adobe 官方地区子集自己切"已被 Fontsource 方案取代，工作量小很多）
- 自托管（本地文件，参考项目现有 PDF 字体的做法），不接入 Google Fonts CDN——因为 Google 域名在中国大陆被屏蔽，而项目是双服务器架构，国际版可以直接用 CDN，但中国版必须走自托管或阿里云自建 CDN，两边需分别处理
- 通过 `unicode-range` 实现按需加载：卡片实际使用到哪些字符，才会触发对应字体文件的下载，不是根据界面语言/地区预先整包加载
- `font-family` 声明末尾必须挂载操作系统自带字体 + Emoji 字体作为兜底（零网络成本，保证文字任何时候都不会不可见）
- `font-display` 策略采用"文字必须立即可见，绝不等待字体加载"（备用字体先顶上，字体加载完成后原地切换）

### 19.3 字体选择菜单——数据结构与交互（本轮重构）

**排查出的历史问题**：早期版本 `resolveFontFamilyForCard` 把用户选中的简中/繁中/日文字体，按固定顺序（`latin, sc, tc, jp, kr`）拼成一条 CSS `font-family` 回退链。这个机制在**简繁中文/日文存在大量共用 Unicode 码位（Han Unification）**的场景下会失控：某语言字体一旦排在回退链靠前的位置，会连带抢占其他语言里跟它共用码位的字符渲染权，导致"选了简体字体，繁体/日文也被连带改变；选繁体或日文，看起来点了没反应（其实生效了，只是被前面的简体字体抢先渲染盖住）"。根因是浏览器的 `font-family` 回退机制处理不了"同码位不同地区字形"的问题，靠回退顺序没法做区分。

**最终方案：手动关联跳转 + 显式优先级链，不做自动语言检测**

- **菜单结构**：五个语言分组（拉丁 latin / 简体中文 sc / 繁體中文 tc / 日本語 jp / 한국어 kr），拉丁与韩语完全独立选择，不参与关联；简中/繁中/日文三者之间做关联
- **取消"系统默认"选项**：原先菜单里的"系统默认"（对应空字符串技术名）已删除，衬线字体本身就是每个语言分组的默认选中项
- **取消"完成"按钮**：选中字体立即生效，面板不自动关闭，靠 `BottomSheetPopover` 组件自带的"点击遮罩关闭"行为来关闭菜单，方便用户连续试多个字体、对比效果再决定
- **每个字体选项各自独立声明完整关联链，不做自动推导**：不是"建一个家族、往里面分组"，而是简中/繁中/日文三方的**每一个字体选项自己身上**，独立写死一份"点击我时，另外两个语言各自跳到哪个字体、以及最终渲染优先级顺序"的完整清单。三条记录各自独立存在，互不引用、互不依赖，改一条不影响其他任何一条：

  ```
  简中 思源宋体   点击后完整链：思源宋体 → 思源宋體 → 源ノ明朝
  简中 思源黑体   点击后完整链：思源黑体 → 思源黑體 → 源ノ角ゴシック
  繁中 思源宋體   点击后完整链：思源宋體 → 思源宋体 → 源ノ明朝
  繁中 思源黑體   点击后完整链：思源黑體 → 思源黑体 → 源ノ角ゴシック
  日文 源ノ明朝   点击后完整链：源ノ明朝 → 思源宋體 → 思源宋体
  日文 源ノ角ゴシック 点击后完整链：源ノ角ゴシック → 思源黑體 → 思源黑体

  ```

- **顺序规则不是"简体统一规则/日文统一规则"这类通用公式**，纯粹是这六条各自独立配置出来的结果，只是恰好呈现出两条可解释的倾向（仅供理解，不作为自动推导依据）：
  - 简体/繁体作为主字体时，日语固定排最后——中文用户选中文字体，遇到没盖到的字，宁可先用另一个中文字体的字形顶上，也不想优先套用日文字体的字形
  - 日语作为主字体时，排序固定是"日语 → 繁体 → 简体"——日本汉字传统写法与繁体中文字形的亲缘关系比简体更近（简体经过大规模简化，笔画结构差异更大）
- **兜底表（覆盖全部五个语言分组）**：任意字体选项若没有配置完整的关联链（比如以后新增字体但漏配、或者用户压根没手动选过字体），缺失的语言分支落到这张表：

  ```
  latin → Source Serif 4
  sc    → 思源宋体（Noto Serif SC）
  tc    → 思源宋體（Noto Serif TC）
  jp    → 源ノ明朝（Noto Serif JP）
  kr    → 본명조（Noto Serif KR）

  ```

  即：全局默认都是有衬线字体，五个语言分组无一例外。

- **面板初始高亮状态**：用户还没手动选过任何字体时，五个语言分组的衬线选项默认显示为"已选中"（走上面的兜底表），面板不区分"默认态"和"手动选中态"两种视觉——衬线默认字体本身就应该显示为已选中，不是特殊的高亮态
- **未来扩展约定（重要，写死为项目铁律）**：以后每新增一个中文或日文字体，**必须手动为它定义完整的关联链**（点击后要跳转谁、渲染优先级顺序是什么），系统不会自动推导任何关联关系。这是可扩展性设计的必要代价：牺牲"自动化"以换取"每条规则的可预期、可单独修改性"

**存储位置（钉死原则）**：`MindCardCardStyle.fontFamily` 结构为：

```typescript
interface MindCardFontFamilySelection {
  latin?: string;
  cjkChain?: string[]; // 完整关联链，发布那一刻钉死存入
  kr?: string;
}

```

点击某个 CJK 字体时，把当时算出的**完整链**（不是只存被点的那一个技术名）整个存进 `cjkChain`，跟项目一贯的"生成那一刻钉死，后续配置修改不影响已发布内容"原则一致——以后就算关联规则改了，已发布卡片的显示效果不会跟着变。CSS `font-family` 渲染时最终按拉丁 → cjkChain（原样展开）→ 韩语 → 系统兜底栈的顺序拼接。

### 19.4 字体切换交互（进度条）

- 用户点击某个字体选项后，若该字体尚未加载，选项框呈现进度效果：灰色蒙版从左到右退去
- 进度为**近似值动画**，不做真实字节级追踪；动画持续推进（可用越接近终点越慢的缓动曲线），但"到达 100%"这一时刻必须与字体真正加载完成的时刻严格对齐，不能提前也不能滞后
- 字体真正加载完成后方可使用

### 19.5 文字背景色 / 卡片背景色

- **文字背景色**：选区层功能，只作用于选中的文字。跟文字颜色共用同一套 8 个 iOS 系统色（黑 #000000/红 #FF3B30/橙 #FF9500/绿 #34C759/蓝 #007AFF/紫 #AF52DE/粉 #FF2D55/白 #FFFFFF）。技术实现上**不需要额外的** `@tiptap/extension-highlight` **包**——`@tiptap/extension-text-style`（跟"文字颜色"共用同一个已安装的包）自带一个 `BackgroundColor` 具名导出
- **图标语言**：文字颜色用"染色的 A + 下方一道同色色条"；文字背景色用"A 背后垫一块圆角色块"
- **卡片背景色**：整卡片层功能，只有**黑、白两个选项**，没有"默认跟随主题"这一档——早期设计曾包含空字符串代表的默认色，但默认色在浅色模式下渲染出来的颜色和纯白视觉上难以区分，实际会造成"黑/白/白"的重复观感，故拍板去掉默认档，只保留黑白二选一
- 卡片背景色图标是一个 3:4 比例的迷你彩色矩形（呼应卡片本身的比例）
- 字体/文字颜色/文字背景色/卡片背景色四者共用同一套 `BottomSheetPopover` 外壳；**四者的选择行为统一为"选中即生效、不自动关闭面板"**，靠 `BottomSheetPopover` 自带的点击遮罩关闭行为来关闭，不再各自维护"完成"按钮

## 二十、命名

- 中文：**片语**
- 日语：**片語**（へんご，真实存在的日语词汇，词典释义与"片言"相同，语义贴合）
- 韩语：**편어**（真实存在但偏书面/生僻，非日常口语高频词，风格上与"片语"本身偏文言的调性算是统一）
- 拉丁语系：暂缓，未找到合适的展示位置

## 二十一、待施工前拟定（不需要用户决策，但施工时需明确）

- `mind_cards`/`mind_card_metrics`/`mind_card_views`/`mind_card_likes`/`mind_card_favorites` 的具体字段细节与索引
- 时间新鲜度衰减曲线的具体形式
- 独立路由的具体路径
- 已读记录清理定时任务的具体清理窗口大小
- 各字体候选（尤其手写体）的最终确认，由用户自行在 Google Fonts 网站预览后决定
- 翻译功能（见第二十六节）的具体接入方式、API key 申请与环境变量配置——方向已定（谷歌云翻译，通用工具，国际服务器专用），尚未实现

## 二十二、施工进度

- **第一轮（Claude Code 施工）已完成**：后端全套（隐私分级/相似度算法/排序公式/召回机制/已读清理定时任务）+ 第一版前端（横向轮播浏览页/圆弧菜单栏/自托管字体系统/PDF 字体换血/仪表盘入口卡片），并完成"思绪卡片→片语/片語/편어"全局改名
- **第二轮（Claude Code 施工）已完成**：编辑界面从"点击外部会关闭的弹窗"重构为独立路由页面（`/dashboard/mind-cards/compose`，物理位置在 `(os)` 路由组外，同报告页既有约定，不带 Dock 导航栏），发布成功后 `router.replace` 返回片语主页
- **第三轮（本 Project 对话内直接生成代码，未经 Claude Code）**：编辑界面的样式数据结构、富文本编辑器技术选型、工具栏视觉与布局的多轮迭代调整，见第二十三节
- **第四轮（本 Project 对话内直接生成代码，未经 Claude Code）**：字体系统从"简单回退链"重构为"手动关联跳转+显式优先级链"（第十九节 19.3）；卡片无限排列生长与三种呈现形态（浏览态裁切渐隐/展开弹窗/编辑画布实时生长，第十七节 17.4、第二十五节）；编辑画布技术实现的多项教训与修复（contenteditable 不可与 flex/grid 布局系统直接结合、点击定位统一改为 JS 计算、进页面聚焦改用首次按键接管+假光标，见第二十六节）；竖排书写方向与对齐绑定、竖排拉丁字母/数字正立堆叠（第十六节 16.2）；讨论并定下翻译功能方向（第二十六节），尚未实现
- **第五轮（Claude Code 施工）已完成**：卡片集功能（收藏夹/自建卡片夹/订阅/个人页三栏），见第二十八节。同轮顺带修复了 `MindCardBody` 对残缺 `style` 数据（老数据 `style` 列非 NULL 但缺 `card`/`runs` 字段）的兜底逻辑，改为按字段各自兜底而非整对象兜底

## 二十三、编辑界面实现（第三、四轮调整成果）

### 23.1 富文本编辑器技术选型：Tiptap

- 选区级加粗/斜体/下划线/文字颜色/文字背景色，需要真正的文本选区检测、局部标记渲染、IME（中日韩输入法组字）正确性、以及"无选区时点击格式按钮，影响接下来输入的新文字"（Word 式行为）——最终选择引入 **Tiptap**（基于 ProseMirror 的 headless 编辑器库），不手写 Selection/Range API
- 只装最小 schema：`Document`/`Paragraph`/`Text`/`HardBreak` + `Bold`/`Italic`/`Underline`/`TextStyle`/`Color`/`BackgroundColor` + `History`（撤销/重做，最小 schema 不含 `StarterKit` 时容易漏装，`History` 不是编辑器自带能力，必须显式装 `@tiptap/extension-history`）
- 不用 `StarterKit`，不允许标题、列表、多段落
- **字体不是 Tiptap mark，不进文档数据结构**：字体是整卡片层状态（`MindCardCardStyle.fontFamily`），不挂在 `textStyle` mark 上，不需要额外的 Tiptap 字体扩展包
- IME 正确性和"无选区时影响后续输入"（stored marks）由 ProseMirror 原生支持，不需要自己实现
- 不用自带工具栏 UI，按钮完全自定义

### 23.2 样式数据结构：两层

```typescript
// 整卡片层：始终整张卡片统一，不随选区变化
interface MindCardFontFamilySelection {
  latin?: string;
  cjkChain?: string[]; // 完整关联链，发布那一刻钉死，见19.3
  kr?: string;
}

interface MindCardCardStyle {
  fontSize: 1|2|3|4|5;
  fontFamily: MindCardFontFamilySelection;
  vertical: boolean;
  align: 'left'|'center'|'right';
  valign: 'top'|'center'|'bottom';
  backgroundColor: '#000000' | '#FFFFFF' | '';  // ''已不再作为可选项提供，但类型层面尚未收紧（见第二十四节待办）
}

// 选区层：带样式标记的文字片段数组
interface MindCardRun {
  text: string;
  bold?: boolean;
  italic?: boolean;
  underline?: boolean;
  color?: string;
  backgroundColor?: string;
}

```

- 发布时：遍历 Tiptap 文档节点，转换成 `runs` 数组（单向转换，草稿箱/编辑已发布卡片不在本次范围内，不需要反向"runs→Tiptap 文档"的水合逻辑）
- `mind_cards.content`（纯文本列）= `runs.map(r => r.text).join('')`，后端自动派生
- **默认卡片样式**：`{ fontSize:3, fontFamily:{}, vertical:false, align:'center', valign:'center', backgroundColor:'' }`

### 23.3 三层渲染架构（本轮确立，替代早期两层设计）

编辑画布与浏览态只读展示（`MindCardBody`）共用同一套三层结构，函数定义在 `cardStyleCss.ts`：

1. **背景层**（`getCardWrapperStyle`）：只负责背景色和整体尺寸，不持有留白
2. **留白+对齐层**（`getCardPaddedFlexStyle`）：flex 布局 + 10% 内边距 + 左中右/上中下对齐。**只允许出现在普通、非 `contenteditable` 的 div 上**——原因见第二十六节
3. **文字层**（`getCardTextStyle`）：字体/字号/书写方向/竖排文字朝向，横排时补 `width:100%`+`textAlign`

留白比例：**10%**（参考 Word 默认页边距约 12%，片语是短句卡片场景不是密集阅读文档，收紧到 10% 更符合"留白感"调性）。

### 23.4 工具栏布局：九宫格

- 三行工具栏共用同一套 `grid-template-columns: repeat(9, 1fr)` 九等分栅格，撑满整行宽度，九个图标间距处处相等，不用 flex + justify-center/justify-between 分组拼凑
- 分组分隔线通过给特定格子加左边框实现，不额外插入占位元素，不破坏九列等宽的结构
- **第一行**（9 格）：字号视觉参照（3 个逐级放大的"A"）+ 五个字号选择圆点（第一行下方的第二个网格行，只横跨第 1-3 列）| 加粗/斜体/下划线（各 1 格）| 字体/文字颜色/文字背景色（各 1 格）
  - **五个圆点的定位**：三个 A 分别居中于整行的 1/18、3/18、5/18 位置，五个点精确定位在这段范围的 16.6667%/33.3333%/50%/66.6667%/83.3333%（绝对定位 + 计算好的百分比），首尾两点严格对齐 A1、A3
  - **五个圆点的点击热区**：视觉尺寸保持 6px 不变，点击热区单独放大成 24×24px 的透明区域包住小圆点，跟工具栏其他按钮"点击热区撑满格子、视觉高亮只占中间一小块"是同一个思路
- **第二行**（9 格）：左中右（各 1 格）| 上中下（各 1 格）| 横排/竖排/卡片背景色（各 1 格）
- **第三行**：可见度（眼睛，复用第一、二行同一套九列栅格的第 1 格）+ 附带信息三个圆（跨最后 3 列，靠右）

### 23.5 图标选择的最终定案

- **对齐图标**：左中右用 lucide 的 `AlignStartVertical`/`AlignCenterVertical`/`AlignEndVertical`；上中下用 `AlignStartHorizontal`/`AlignCenterHorizontal`/`AlignEndHorizontal`。"Vertical"结尾表示"对齐一条竖线基准"（控制左右位置），"Horizontal"结尾表示"对齐一条横线基准"（控制上下位置）
- **横排/竖排图标**：`TextAlignStart`，竖排版本用 CSS `transform: rotate(90deg)` 旋转
- **文字颜色图标**：染色的"A" + 下方一道同色色条
- **文字背景色图标**："A"背后垫一块圆角色块
- **选中态视觉**：图标选中态用固定尺寸（28px）的圆形灰色徽章包裹图标，点击热区与视觉高亮圆分离成两层
- **不做图标名称/悬停提示**：图标视为通用符号，不加 `title`/`aria-label`

### 23.6 附带信息三个圆的当前实现

- 命主：仅 UI 占位，未接后端（没有对应的现成数据源）
- 八字：对接现有 `attachWuxing`
- 大五：对接现有 `attachBigfive`

## 二十四、已知未闭环事项（收尾时明确记录，不假装已解决）

- `MindCardBackgroundColor` **类型尚未收紧**：卡片背景色选择器已经去掉了"默认跟随主题"选项，只提供黑白两色，但底层类型定义仍保留着空字符串这个值，未同步收紧
- **SQL 迁移** `mind_cards.style jsonb` **列**：迁移本身已执行，但卡片集功能施工验证时发现至少一行历史遗留数据的 `style` 列非 NULL、却缺 `card`/`runs` 字段（推测是该列刚建出、尚未补齐默认值时创建的卡片）。未对数据做回填，改为在 `MindCardBody.tsx` 按字段各自兜底（`style?.card ?? DEFAULT_MIND_CARD_CARD_STYLE`、`style?.runs ?? []`，而不是整个 `style` 对象一起兜底），这类残缺行现在能正常渲染成默认样式
- **PDF 导出字体换血**：已由 Claude Code 完成，并做过实际生成 PDF+文本回读的验证（五种文字脚本回读结果一致）
- **字体资源体积**：自托管字体（网页端 + PDF 端）合计约 103MB 已进入代码仓库，如后续觉得仓库体积不可接受，可考虑改用 Git LFS 或迁移到 Cloudflare R2
- **编辑画布基准尺寸（400px）是估算值**：横板宽度固定、竖版高度固定所用的基准参考值（`CARD_BASE_DIMENSION_PX = 400`），未经真机/浏览器实测确认，是否需要调整待用户实际使用后反馈
- **切换横板/竖版时画布形状会跳变（已知的设计代价，非缺陷）**：横板固定宽、高度自由生长；竖版固定高、宽度自由生长——两个方向互斥，切换时"已经长出来的那部分"不会带到新方向上，是重新按新方向规则从头排。内容越长跳动越明显。已如实告知用户，暂不做过渡动画缓解，需要时可再讨论
- **假光标（FakeCaret）的精确定位是近似值**：复用 `getCardPaddedFlexStyle` 的对齐规则定位，对绝大多数场景准确，但未做逐像素级别的校准
- **翻译功能未实现**：方向已定（谷歌云翻译 Google Cloud Translation API，通用工具设计——片语/私聊/未来论坛共用同一套，只接入国际服务器），需要单独申请谷歌云账号与 API key（跟现有 `GEMINI_API_KEY` 不是同一入口，不能共用），具体接入方式待展开讨论
- **留言、通知系统、草稿箱**：均仍是规划阶段，未开工。收藏夹分组管理已完成（见第二十八节），草稿箱的前置依赖已满足；通知系统仍依赖留言等具体事件先存在（收藏产生的通知除外，已作为卡片集功能的一部分最小化实现——只写入 `mind_card_favorite_notifications` 表，没有通知中心UI，见第二十八节）
- **手写体候选**：暂缓，用户自行去 Google Fonts 网站预览决定
- **卡片集订阅功能未实测**：收藏多选窗口/新建卡片夹/圆弧菜单跳转/编辑与删除卡片夹均已冒烟测试通过，但订阅/取消订阅这次先跳过，后续需要单独验证
- **`folder_kind='journal'`（记录型）完全未实现**：数据库字段值预留，前端新建卡片夹界面不提供入口（只有一个禁用态的"记录型（即将推出）"占位选项，纯展示不可选中），后端也不接受该值

## 二十五、卡片无限排列与三种呈现形态

片语卡片支持"朝一个方向无限排列"：横板向下无限排列，竖版向左或向右无限排列（取决于对齐，见 16.2）。这个特性在三个不同场景下有三种不同的呈现方式，互不影响：

### 25.1 浏览态缩略图（`MindCardBody` clipped=true）

- 固定 3:4，`overflow:hidden` 裁切超出内容
- 触碰无限排列时，在"生长方向那一侧"边缘加文字渐隐遮罩（`mask-image` 线性渐变），只渐隐文字层，不渐隐背景层
- 生长方向与渐隐边缘对应关系：横排→渐隐底部；竖排左对齐（`vertical-lr`）→渐隐右侧；竖排右对齐/居中（`vertical-rl`）→渐隐左侧
- 是否需要渐隐由 JS 实际测量文字内容尺寸 vs 容器可用尺寸判断（`ResizeObserver` + 内容签名比对），不是静态开关

### 25.2 展开详情弹窗（`MindCardDetailModal`，点击浏览态卡片触发）

- 基础尺寸仍是 3:4，但**锁定生长方向对应的那个维度**：横板锁宽、竖版锁高，另一个维度在弹窗内部滚动（`overflow-y:auto` 或 `overflow-x:auto`），弹窗容器本身尺寸不随内容变化
- 弹窗内渲染不裁切、不加渐隐遮罩（这是"完整展开"状态）
- 未来的点赞/留言/收藏等交互，都会在点击展开后的弹窗里出现（当前只有点赞/收藏，留言等待第十五节的留言子模块单独设计后再接入，本次不预留占位）

### 25.3 编辑画布（`RichTextComposer`，实时生长，见第二十六节）

- 不是固定 3:4，是所见即所得的实时生长画布——横板宽度固定、高度随内容自由生长；竖版高度固定、宽度随内容自由生长
- 具体实现见第二十六节

## 二十六、编辑画布实现要点与技术教训

这一节记录编辑画布（`RichTextComposer.tsx`）实现过程中反复验证过的技术约束，避免以后重蹈覆辙。

### 26.1 `contenteditable` 元素本体绝不能直接是 flex 或 grid 容器

- **已用 WebKit 官方 bug 追踪记录核实**：`display:flex` 直接加在 `contenteditable` 元素本身上，在多个浏览器场景下会导致光标定位异常、甚至完全无法输入；`display:grid` 同样有记录在案的 bug，尤其是"元素为空时无法编辑"这个场景（跟编辑器刚打开、还没打字时的状态完全吻合）
- 这不是某一种布局技术单独的缺陷，是"复杂排版系统 + 可编辑元素 + 空白状态"这个组合本身在浏览器实现层面的通病，换用表格布局（`table-cell`/`vertical-align`）等老派技术也同样有竖排书写模式下的已记录 bug（WebKit bug：竖排书写模式不会让表格单元格真的变成竖排）
- **结论**：`.ProseMirror`（Tiptap 渲染出的 contenteditable 元素本体）只允许设置字体、字号、`writing-mode`、`text-orientation`、`text-align`、`width`（横排时）这类安全的文字样式，**绝不设置** `display`/`flex`/`grid`/`padding` 等布局属性
- 排版对齐（留白 + 左中右/上中下）职责交给一个**外层、非 `contenteditable` 的普通 div**（`getCardPaddedFlexStyle`），普通 div 用 flex 完全安全，跟 `MindCardBody` 只读展示层是同一套函数、同一个安全区

### 26.2 点击定位：不依赖浏览器原生行为，统一由代码计算

- 早期尝试过"点在 `.ProseMirror` 内部交给浏览器原生处理、只有点在外部才手动计算"的分工方式，实测下来浏览器原生表现不稳定，同一套逻辑在不同位置给出不一致的结果
- **最终方案**：不区分点击位置，任何一次点击都统一执行同一段逻辑——把点击坐标夹紧到"留白+对齐层"（`e.currentTarget`）的实际渲染范围内，再用 `editor.view.posAtCoords` 换算成文档里最近的字符位置，显式调用 `editor.chain().focus(pos.pos).run()`；查不到有效位置时兜底 `focus('end')`
- **踩过的坑**：曾经把夹紧范围设成 `.ProseMirror` 自己的渲染范围，但 `.ProseMirror` 的实际大小只取决于已打字数，外层"留白+对齐层"如果比它大（比如画布有最低高度兜底），中间会出现点击死区。必须夹紧到"用户实际看到的可点击框"的范围，不能夹紧到内容本身的范围
- **另一个踩过的坑**：外层"留白+对齐层"用百分比高度（`height:'100%'`）撑满，只有在父级有**明确写死的固定高度**时才会真正生效；父级如果只是"最低高度兜底"（`minHeight`，不是定死的 `height`），百分比高度会静默失效，这层实际缩回到只跟内容一样大。修法：让父级（编辑画布外壳）本身是 flex 容器，子层用 `flex:1` 撑满，不依赖百分比高度这个在特定条件下会失效的机制

### 26.3 进页面自动聚焦：已知在此技术栈下不可靠，改用替代方案

- **已用 Tiptap 官方仓库 issue 记录核实**：`autofocus` 选项自某个版本起在 React 场景下存在已确认的失效问题；即便绕开这个选项、手动调用聚焦命令（不管是绑在组件 `useEffect`、编辑器 `onCreate` 生命周期，还是延迟一帧/延迟固定时间再执行），在本项目"Next.js + `immediatelyRender:false`"的组合下都反复验证失败，光标不出现
- 推测根因：浏览器对"没有搭配真实用户操作的程序化聚焦"本身就区别对待（安全/体验考量，防止网页乱抢焦点），这不是能靠再调整一次时机解决的问题，是这条路径在这套技术组合下走不通
- **最终采用的替代方案（两件事叠加使用）**：
  1. **全局键盘监听接管首次按键**：监听 `window` 的 `keydown` 事件，只要检测到用户开始敲键盘（且当前焦点不在别的输入框上、不是快捷键组合），立刻调用 `editor.commands.focus('end')` 把这次按键接到编辑器——因为绑在真实用户操作（按键本身）上，浏览器对这类场景的支持稳定得多，这是"进页面直接打字，不需要先点击"这个核心需求的真正实现方式，不依赖"提前显示光标"这件事本身是否成功
  2. **假光标（`FakeCaret`）**：`editor.isEmpty` 为真时，在"留白+对齐层"上叠加一条纯视觉、CSS 动画驱动的闪烁竖线（横排）/横线（竖排，因为光标本身应跟阅读方向垂直），复用跟真实文字完全相同的对齐规则定位（`getCardPaddedFlexStyle`），不是真正在编辑，只是提前告诉用户"这里能打字"。用户敲下第一个字，`isEmpty` 立刻变 false，假光标自动消失
- 这套组合已经满足产品实际诉求（"进页面就能直接打字，不需要先点一下"），不再追求"真实编辑器光标提前闪烁"这个更严格的效果

### 26.4 编辑画布尺寸：一个方向固定、一个方向自由生长

- **两个曾经踩过的具体理解错误，记录以避免重犯**：
  1. 块级元素的 `width:'auto'` **不等于**"按内容收缩"，真实含义是"撑满父容器能给的宽度"——要真正做到"按内容收缩/生长"，必须显式声明 `width:'fit-content'`
  2. `aspect-ratio` **不会**让一个普通块级盒子"内容超出就把盒子撑大"——它只是划定一个固定比例的框，内容超出时是溢出到框外面（视觉上看起来像文字探出卡片背景），盒子本身不会变大。要让盒子跟着内容真正生长，只能用最朴素的做法：`height:'auto'`（普通盒子的高度默认就是被内容撑开的）+ `minHeight` 作为"内容很少时"的下限兜底
- **最终实现**（`CARD_BASE_DIMENSION_PX = 400`，`CARD_BASE_CROSS_PX = 400 × 4 / 3 ≈ 533.33`）：
  - 横板：`width:'100%', maxWidth:400, height:'auto', minHeight:533.33`（宽度固定，高度内容驱动+下限兜底）
  - 竖版：`height:533.33`（固定，不随内容变化）`, width:'fit-content', minWidth:400, maxWidth:'90vw'`（高度固定，宽度按内容收缩/生长，minWidth 兜底避免内容极少时太窄，maxWidth 防止内容极多时撑爆视口）

## 二十七、翻译功能（方向已定，未实现）

- **服务选型：Google Cloud Translation API**（不是复用 Gemini）。已对比稳定性——Gemini 作为通用生成式大模型接口，复杂度高、有已记录的异常/挂起案例，且模型版本迭代频繁；Google Cloud Translation 是功能单一、成熟多年的专用翻译服务，结构性上更稳定。片语场景对翻译本身不是强实时刚需，Gemini 的稳定性劣势原本可以接受，但用户最终拍板选择专用翻译服务
- **双服务器隔离**：只接入**国际服务器**，中国大陆服务器不装这个功能——同时天然绕开"谷歌服务在中国大陆被墙"的问题，不需要为此额外找代理或自建方案
- **通用工具定位**：不是片语专属功能，设计为一个独立的、可被多处复用的翻译能力——片语卡片、未来的私聊、未来的论坛，都调用同一套翻译逻辑，不为每个场景各写一份
- **成本**：Google Cloud Translation 有永久性的每月 50 万字符免费额度（不是限时优惠），超出部分约 20 美元/百万字符。片语卡片是短句场景，实际用量下的成本可以忽略
- **需要单独申请**：Google Cloud 账号 + 单独的 API key，跟项目现有的 `GEMINI_API_KEY`（走 Google AI Studio 入口）不是同一个入口，不能共用，需要新增环境变量
- **与现有"UGC 翻译"决策的关系**：`CLAUDE.md` 与本文档此前已记录"UGC 翻译：临时生成、不入库缓存"的原则性决策，这次是把这个原则具体落实到"用哪个服务"这个技术选型上，不是新的决策方向
- 具体接入位置、请求节流、失败兜底（翻译失败时的降级显示）等实现细节，留待正式施工前再展开讨论

## 二十八、卡片集（收藏夹 / 自建卡片夹 / 订阅 / 个人页三栏）

### 28.1 核心概念

- **"我的卡片"**：不是真实卡片夹，是虚拟视图（`mind_cards WHERE user_id = 当前用户`），不落表，不能改名/删除/被订阅
- **"收藏夹"**：`mind_card_folders` 的一行，`is_default = true`，随账号创建时由 `handle_new_user()` 触发器同步建出，不可删除、不可改名、不可改介绍（第二轮补上的限制，见 28.8），但可见度和排序仍可修改。除此之外和用户自建夹完全平等（一样能被订阅、一样能设可见度、一样能拖拽排序）
- **收藏图标点亮态**：不是独立布尔字段，是派生结果——这张卡片在当前用户名下任意一个卡片夹（含收藏夹）里存在关联记录，即为"已收藏"

### 28.2 数据库表

- `mind_card_folders`：卡片夹主表。`folder_kind` 目前只使用 `'collection'`（`'journal'` 记录型字段值预留，未实现任何逻辑）；`display_mode`（`'album'` 册子 / `'stack'` 集子）创建时选定，写入后接口层拒绝修改；`visibility` 复用卡片本身的四档语义（`private`/`followers`/`friends`/`public`）；`is_default` 一个用户唯一一行为 `true`（部分唯一索引 `mind_card_folders_one_default_per_user`）；`description` 为施工阶段新增的可选字段，供册子型封面页使用
- `mind_card_folder_items`：卡片夹内容关联表，组合主键 `(folder_id, card_id)`，不做 RLS（与既有 `mind_card_*` 五张表策略一致，隐私过滤全部在 API 层手动处理）
- `mind_card_folder_subscriptions`：订阅关系表，组合主键 `(subscriber_id, folder_id)`
- `mind_card_favorite_notifications`：收藏通知最小表（`card_owner_id`/`actor_id`/`card_id`/`read_at`），只在"该卡片对该操作用户的夹计数从 0 变为 1"时插入一行，自己收藏自己的卡片不触发。**只写入，不读取**——通知中心 UI 仍是圆弧菜单栏右侧的占位入口（见第十八节），本轮不解除占位

### 28.3 `handle_new_user()` 触发器更新

注册时除了原有的 `insert into public.users`，同步插入一行默认收藏夹（`folder_kind='collection'`、`display_mode='album'`、`visibility='private'`、`is_default=true`）。已为施工前所有老用户执行一次性补建脚本（先 `SELECT` 确认缺失名单，再 `INSERT`，避免误建重复默认夹），补建后验证：所有用户的 `is_default=true` 卡片夹行数均恰好为 1。

**`name` 字段的哨兵值约定（第二轮修订，见 28.8）**：默认收藏夹的 `name` 不再存字面量 `'收藏夹'`，改存空字符串 `''` 作为哨兵值，前端按 `is_default=true` 走 `mindcards.folders.default.name` 这个 i18n key 实时渲染显示名。选 `''` 而不是 `NULL`，是为了不必对 `mind_card_folders.name`（`NOT NULL`）做 `ALTER TABLE`。

### 28.4 API 一览

- 卡片夹 CRUD：`POST /api/mind-cards/folders`、`PATCH /api/mind-cards/folders/:id`、`DELETE /api/mind-cards/folders/:id`
  - PATCH 允许字段：`name`/`visibility`/`order_index`/`description`。`description` 是施工时的实用决定——需求文档的 PATCH 清单里没写它，但这是它唯一的编辑入口，不放行就等于这个字段永远没法被设置
  - `is_default=true` 的夹：`name`**和**`description` 变更均静默忽略（第二轮补上 `description` 的拦截，第一轮只挡了 `name`），`visibility`/`order_index` 仍可改
  - DELETE：`is_default=true` 直接 403；有卡片的夹前端二次确认后才真正删除，级联删除 `folder_items`，卡片本体和卡片在其他夹里的关联不受影响
- 单张卡片：`PATCH /api/mind-cards/:id`（仅允许改 `visibility`，选中即生效不需二次确认，不接受 `content`/`style`——内容编辑明确排除）、`DELETE /api/mind-cards/:id`（仅限本人卡片，前端二次确认后调用，级联删除由外键处理）
- 收藏/归类：`GET /api/mind-cards/:cardId/folder-status`（多选窗口用）、`POST`/`DELETE /api/mind-cards/:cardId/folders/:folderId`（首次入夹触发收藏通知）
- 订阅：`POST`/`DELETE /api/mind-cards/folders/:folderId/subscribe`（订阅前校验夹对当前用户可见，否则 403）
- 夹内卡片：`GET /api/mind-cards/folders/:folderId/cards`——双重过滤（卡片夹本身 `visibility` + 卡片自身 `visibility`，两层都通过才返回），游标分页
- 个人页三栏：`GET /api/mind-cards/profile/mine`、`/profile/folders`、`/profile/subscriptions`，均接受可选 `?userId=` 查询参数（省略时默认当前用户）。本轮前端只以自己身份调用（个人页范围定为"仅查看自己"，圆弧菜单左侧按钮直接跳转），但接口本身已支持将来复用于查看他人主页，无需重新设计

### 28.5 前端（第二轮后的现状）

- `FolderMultiSelectPopover`：收藏图标（第二轮改用 `Album` 图标，见 28.8）点击后打开的多选窗口，取代了 MVP 阶段"点一下切换收藏"的简单开关。每次勾选立即生效（无"完成"按钮），点遮罩关闭；新建卡片集走共享组件 `MindCardFolderCreateForm`（见 28.8），新建成功即自动勾选（新建=加入）
- `/dashboard/mind-cards/profile`：个人页三栏（我的卡片 2 列网格 / 卡片集网格 / 订阅卡片集网格）。**第二轮已搬出 `(os)` 路由组**（原本沿用 Dock 导航，现改为左上角纯图标返回按钮 + `router.back()`，与 `compose`/报告页同一先例），物理路径变为 `dashboard/mind-cards/profile/page.tsx`，网址不变。圆弧菜单栏左侧收藏入口按钮跳转到这里
- `FolderBrowseView`：夹内浏览。册子型（`album`）分页，每页 4 张，第一页固定为封面（夹名+介绍），翻页用简单的左右箭头（未做手势滑动，范围克制）；集子型（`stack`）用 `framer-motion` 的 `useScroll`+`useTransform` 按滚动进度驱动每张卡片的位移/透明度/缩放，完全由 `scrollYProgress` 派生，天然可逆（"向下滚动逐张飞开，回滚逐张飞回"）
- 卡片集管理（个人页"卡片集"栏，第二轮重构为网格，见 28.8）：编辑（改名/介绍/可见度，`is_default` 夹隐藏改名/介绍框）、删除（非默认夹，有内容时二次确认）、拖拽排序（含默认收藏夹）。**创建入口有两处，共用同一份表单组件 `MindCardFolderCreateForm`**：多选窗口内的"新建卡片集"、个人页"卡片集"栏网格末尾的虚线 `+` 方框

### 28.6 与 MVP 阶段收藏机制的关系（迁移决策）

MVP 阶段的 `mind_card_favorites`（不分组收藏）**从未正式上线使用，无真实用户数据**，因此退役时跳过了原计划的"迁移到默认收藏夹→核对行数→再删表"三步流程，直接 `drop table`。`following`/`recommend` 两个 tab 原本查 `mind_card_favorites` 判断"是否已收藏"的地方，改为调用共享函数 `computeFavoritedSet`（`lib/mindCards/favorites.ts`）——按 viewer 名下所有卡片夹（含默认夹）派生，这样"收藏点亮态"在全站所有入口（关注/推荐/夹内浏览/个人页我的卡片）语义完全一致，只有一份判定逻辑。

### 28.7 施工时顺带修复的历史遗留 bug

个人页"我的卡片"栏是第一个会渲染"当前用户自己发布的卡片"的入口——由于关注/推荐两个 tab 天然不会展示用户自己的卡片给自己看（不会关注自己），这类卡片此前从未被任何界面渲染过。冒烟测试时暴露出至少一行历史遗留数据的 `mind_cards.style` 列非 NULL、却缺 `card`/`runs` 字段（推测是该列刚建出、尚未补齐默认值时创建的卡片），导致 `MindCardBody` 崩溃。根因是原有兜底逻辑 `style ?? DEFAULT_MIND_CARD_STYLE` 只处理"整个 `style` 为 null/undefined"这一种情况，对"`style` 是非空但残缺的对象"无效。修复为按字段各自兜底（`style?.card ?? DEFAULT_MIND_CARD_CARD_STYLE`、`style?.runs ?? []`），未对历史数据做回填，这类残缺行现在会正常渲染成默认样式。

**同一个坑踩了第二次**：`MindCardBody` 修完后，`MindCardDetailModal.tsx` 里独立算横竖版尺寸用的 `card.style?.card.vertical` 又崩了一次——第一层 `?.` 有加，但链到 `.card.vertical` 时忘了继续用 `?.`（`card` 本身可能是 undefined）。修法一样：`card.style?.card?.vertical ?? false`。这类"同一个可能残缺的字段，在多个文件里各自单独访问"的场景，必须每一处都独立检查兜底是否到位，改完共享组件不代表所有直接访问点都安全。

### 28.8 第二轮细化（外观/术语/权限调整，不改核心数据模型）

**图标改动**（lucide）：圆弧菜单栏左侧入口 `Bookmark`→`Book`；卡片上打开多选窗口的收藏按钮 `Bookmark`→`Album`；默认收藏夹在列表/网格里的身份徽标改用 `BookHeart`（贴在 3:4 展示框左上角，是"这是默认收藏夹"的身份标记，不是"类型"信息，因此不受下面"展示框不显示类型图标"的规则约束）；收藏册（`album`）用 `BookText`，明信片（`stack`）用 `BookImage`，随笔册（`journal`，仍是禁用占位）用 `Notebook`——这三个类型图标只出现在新建/编辑表单里，不出现在展示框上。

**术语改名**（仅界面文案，数据库字段名、TS 类型名、i18n key 顶层前缀均保持不变，`MindCardFolder`/`mindcards.folders.*` 都没有改成 `Collection`/`collections.*`）：卡片夹→卡片集、订阅卡片夹→订阅卡片集、册子→收藏册、集子→明信片、记录型→随笔册。

**点赞功能整体退场**：`mind_card_likes` 表在正式上线前确认无真实数据后直接 `drop table`（同 28.6 对 `mind_card_favorites` 的处理原则）。`MindCardCarousel`/`MindCardDetailModal`/`FolderBrowseView`/各 feed 路由（`following`/`recommend`/`profile/mine`/`folders/:id/cards`）里的 `liked`/`onToggleLike` 字段和逻辑全部移除，不是换皮保留。`/api/mind-cards/:id/like` 路由已删除。

**"我的卡片"新增操作**：卡片网格新增删除（`DELETE /api/mind-cards/:id`，前端二次确认）和改可见度（`PATCH /api/mind-cards/:id`，仅接受 `visibility`，选中即生效不需二次确认）两个入口，均以小图标叠加在卡片右上角。内容重新编辑明确不做。

**卡片集展示重构为 3:4 网格**：卡片集/订阅卡片集两栏从原来的列表行改为统一的 3:4 展示框网格（`MindCardFolderCard` 组件），框内只有名称 + 封面，**明确不显示**数量、类型图标、可见度标识。封面取该夹内**最新一张卡片的内容**，复用 `MindCardBody` 的 `clipped` 裁切渲染，纯文字/HTML 渲染，不涉及图片存储（`mind_card_attachments` 依然不建）。后端 `profile/folders`、`profile/subscriptions` 两个接口不再返回 `item_count`，改为返回 `latest_card`（`{id, content, style}` 或 `null`），由共享函数 `fetchLatestCardByFolder`（`lib/mindCards/folderCover.ts`）一次查询算出每个夹的最新卡片，不做 N+1：先按 `added_at` 倒序拉全部相关 `folder_items`，JS 里按 `folder_id` 去重取第一条，再一次性查这些 `card_id` 对应的卡片内容。

**卡片集拖拽排序**：参照档案管理页（`profiles/page.tsx`）的 `@dnd-kit/core`+`@dnd-kit/sortable` 实现，网格布局用 `rectSortingStrategy`（不是档案页用的 `verticalListSortingStrategy`）。**与档案页的关键差异**：档案页"本人档案锁顶、不参与排序"这条规则**不适用**这里——默认收藏夹和自建卡片集一视同仁，可以被拖到任意位置，没有"排除默认夹"的特殊分支。拖拽结束后静默 `PATCH order_index`。

**默认收藏夹权限矩阵**：改名❌、改介绍❌（均后端接口层拒绝，不只是前端隐藏）、删除❌（沿用第一轮）、拖动排序✅、改封面✅（自动取最新卡片，没有独立的"改封面"操作）、改可见度✅。

**默认收藏夹名称改走 i18n**：见 28.3 的哨兵值说明。个人页/多选窗口/夹内浏览页顶栏等任何渲染卡片集名称的地方，统一逻辑是"`is_default=true` 时不读 `name` 字段，改渲染 `mindcards.folders.default.name` 的当前语言翻译；否则渲染 `name` 字段本身（用户自由文本，不走 i18n）"。

**个人页搬出 `(os)` 路由组**：见 28.5。这是继报告页之后第二次利用"路由组对不需要 Dock 导航的独立页面"这个既有机制，佐证了 CLAUDE.md 里"这个设计此前长期没被真正用到"的记录已经过时——现在有两个真实用例了。

**去掉圆弧菜单栏的悬浮提示**：`title`/`aria-label` 从收藏/发布/通知三个按钮上一并移除，把"图标视为通用符号、不加文字提示"这条此前只覆盖编辑工具栏九宫格图标的规则（见 23.5），扩大到圆弧菜单栏。

**共用组件抽取**：`MindCardFolderCreateForm`（新建卡片集表单，多选窗口和个人页"卡片集"栏的 `+` 入口共用同一份，不重复实现）、`MindCardFolderCard`（3:4 展示框，卡片集/订阅卡片集两栏共用）。

**已知未闭环（本轮新增）**：
- 订阅/取消订阅功能本轮冒烟测试未覆盖
- 个人页范围仍是"仅查看自己"，没有任何"查看他人卡片集/发起订阅"的入口，`subscription.subscribeButton` 这个翻译键目前没有对应的可点击 UI
- `mindcards.folderActions.edit`/`deleteDefaultBlocked`/`renameDefaultBlocked`/`myCards.changeVisibility` 几个翻译键按需求文档要求已建好，但没有任何地方触发展示这些文字（图标即符号不加文字提示；默认夹的改名/介绍输入框和删除入口都是直接不渲染，不是禁用态+提示文案）。`renameDefaultBlocked` 原本会在编辑表单里点击改名后弹出提示，体验反馈"点了才告诉你不行"，已改为默认夹的编辑表单直接不渲染改名/介绍两个输入框（见 28.9）

### 28.9 用户反馈修复（可见度切换即时反馈 + 默认夹改名限制体验）

**改可见度点击后没有立即反馈**：`MindCardsProfileView` 原来把"当前正在改可见度的卡片"存成一份独立的 state 快照（`visibilityTarget: MindCard | null`），点击选项后虽然 `mineCards` 数组本身更新了，但这份快照没有跟着变，导致弹窗里的高亮选中态停留在旧值，用户以为"点了没反应"（实际数据库已经写入成功）。**修法**：改成只存 `id`（`visibilityTargetId: string | null`），弹窗要渲染的对象每次都从 `mineCards` 里现查（`mineCards.find(c => c.id === visibilityTargetId)`），保证只有一份真相来源——`mineCards` 一更新，派生出来的弹窗内容立刻跟着变，不需要在两个地方分别维护同一份数据。

**默认收藏夹改名限制的体验调整**：原来的做法是编辑表单里展示一段说明文字（"收藏夹不能改名"），点开编辑表单才看得到。用户反馈这不符合"限制应该体现在操作压根不可用，而不是点了才告诉你不行"——跟圆弧菜单"图标是通用符号不加tooltip"是同一个原则。修法：`is_default=true` 的编辑表单里，改名输入框和介绍输入框直接不渲染（保留一行纯文字展示当前显示名，非输入框样式，不可点击），彻底去掉那句解释性文案，`mindcards.folderActions.renameDefaultBlocked` 这个 i18n key 保留在翻译文件里作为预留字符串，代码里不再有任何触发点。
