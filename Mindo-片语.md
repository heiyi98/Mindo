# Mindo 片语模块 — 架构决策文档

## 一、模块定位

- 独立模块，与论坛无关联，不复用论坛的治理/权限体系
- 类似"一言""平行世界"：用户发布一段文字，可选择性附带个人命理/心理量化信息
- 用户可刷到其他人发布的卡片，排序由"新鲜度 + 人格相似度"共同决定（推荐算法完整设计见第三十四节，尚未实装）
- 提供"关注"与"推荐"双栏入口（见第九节）

## 二、内容与形态

- 当前版本：纯文本 + 基础富文本（见第十六节），不含媒体附件
- 远期：支持图片、音频等媒体附件（架构需预留，见"数据分层"一节）；音频专属集子（辑/album）、绘画专属集子（廊/gallery）见第三十节命名体系
- 卡片渲染用普通 HTML + CSS，不用 SVG

## 三、隐私档位

四档，存在卡片主表的一个字段上：公开/仅粉丝可见/仅好友可见/仅自己可见。隐私档位过滤对"推荐""关注"两个入口都必须生效，不因入口不同而放宽。

**铁律（第三十四节推荐算法重构时确立）**：可见度检查必须焊死在"取卡片"这唯一一个入口函数内部，不允许由每个使用场景（推荐/关注/卡片集/我的卡片）各自调用检查——场景越多，"某处忘了检查"这类漏洞的风险越大。任何新场景想要卡片，只能走这一个入口，过滤逻辑在这里做一次，不允许绕开。这个函数只回答"你是谁、这批里哪些你有资格看"，不关心"你是为了什么目的来的"——"目的"（推荐用相似度排、关注用时间排）是过滤完之后的下一层，两层严格分开。

## 四、个人信息关联方式

发布时用户主动选择带上的字段值本身（原始数值，不是已经翻译成某种语言的文字），直接复制进卡片关联的数值记录里，不是实时引用当前档案、也不是整份档案快照。与 `bazi_readings` 的"生成那一刻钉死"原则一致。

## 五、数据分层原则（极简 + 解耦 + 接口隔离）

三层分离：`mind_cards`（卡片本体）/ `mind_card_attachments`（媒体附件，预留未建）/ `mind_card_metrics`（量化数值快照）。

## 六、相似度算法（基础版，已实装；完整推荐算法重构见第三十四节）

五行、大五各自独立成一个"因子"，当作 N 维空间的点，欧氏距离换算成 0~1 相似度。多因子加权平均，权重按"当前激活了几个因子"平分。

## 七、总排序公式（基础版，将被第三十四节的四线并行方案取代）

`60% 相似度 + 40% 新鲜度`，这是旧的混合评分公式。**第三十四节确立的新方案会取代这个公式**，不再是"混合成一个分数"，而是"用不同机制各自独立选卡，拼成一个推荐卡组"。

## 八、候选人召回机制（推荐 tab 专用）

已读改为**降权不剔除**（打 3 折），不再整个过滤掉——数据量小的阶段，硬性剔除会导致候选池很快刷完。明确排除浏览者自己发布的卡片。

## 九、关注 / 推荐 双栏入口

推荐 tab 走候选池+加权排序（即将被第三十四节取代）+已读降权；关注 tab 是纯时间线，不计算相似度，不受第三十四节改动影响。

## 十、收藏（点赞已下架）

`mind_card_likes` 表已确认无真实数据，直接下架，全站点赞相关 UI/逻辑一并移除。收藏改为"卡片集"体系（见第二十八、三十一节），不再有 MVP 阶段那种不分组的简单收藏表。

## 十一、双服务器隔离

中国/国际服务器完全隔离。**IP 地区标注功能**（见第三十五节）也遵守这条：只接入国际服务器，中国大陆版另有法定强制要求，两边逻辑不通用。

## 十二、入口设计

仪表盘 1×2 卡片模块跳转至片语独立页面（已搬出 `(os)` 路由组，见 `CLAUDE.md`）。

## 十三～二十七节：MVP 阶段基础架构、字体系统、竖排规则等

（内容与上一版一致，此处不重复列出——收藏册/明信片/随笔册这几个旧名字，已在第三十节被新命名体系取代，卡片集页面的具体实现请以第二十八、二十九节最新版本为准，不要再参照本节可能提到的旧组件结构）

---

## 二十八、卡片集页面重构（第三轮，本轮对话完成）

### 28.1 卡片集封面——只显示名字，不再预览卡片内容

`MindCardFolderCard.tsx`：封面永远只显示卡片集名字（居中，项目默认衬线字体），不再取夹内最新卡片渲染预览。左上角"默认收藏夹"徽标（`BookHeart`）已移除。右上角编辑/删除图标已移除——管理操作统一收进"点进卡片集之后的详情页"，缩略图本身只负责点击跳转。

### 28.2 卡片集详情页（`FolderBrowseView.tsx`）——信息页与内容页合并成一条横向轨道

- **不是**两个独立组件互相替换，是**同一条宽度 200% 的横向轨道**（`motion.div`），两块各占轨道一半，点击箭头触发的是这条轨道整体的位置动画（`x: 0%` ↔ `x: -50%`），视觉上是"同一个地方挪了位置"，不是两个页面互相跳转
- **信息页**：标题偏上（不绝对居中，`top: 38%`）、简介与标题留白更明显、可见度靠下（`bottom: 16%`）、右侧边缘垂直居中一个"细长胶囊+居中小图标"的进入提示（不是拉伸变形的箭头图标，图标本身不变形，"长"的观感由胶囊形状承担）
- **内容页**：左侧边缘对称的返回箭头（同款胶囊）
- **编辑/删除**：挪进顶部返回键那一栏，跟返回键平行显示，不再单独浮在信息页内容区域
- **顶部返回键语义**：两个屏幕下统一都是"直接退出到卡片集列表"，不再兼职"先回信息页"——这个职责交给内容页左侧新增的箭头专门处理
- **高度测量**：改用一个不参与滑动动画的独立容器（`contentAreaRef`）测量，不透过会做位移变换的轨道去测——**踩过的坑**：透过一个"正在做 CSS 变换"的元素去测量它子元素的实际尺寸，容易测不准，这次对话里反复出现过同类问题，教训见第三十六节
- **收藏册（album）网格**：改用 JS 精确测量，保证 2×2 四张卡片永远完整显示、不需要滚动兜底；四个格子永远固定渲染（卡片不够时用不可见占位撑住），界面整体轮廓不会因为这一页卡片数量不足而跟着缩水变形。留白/间距/翻页按钮预留空间全部用明确的像素常量（`ALBUM_CONTENT_PADDING_PX`/`ALBUM_ROW_GAP_PX`/`ALBUM_PAGER_HEIGHT_PX`），JS 计算和实际渲染共用同一份数字，不再靠猜 Tailwind 类名对应多少像素
- **明信片（stack）**：不再是"上下滚动依次飞开"的效果，改为复用主浏览页同一套横向轮播组件（`MindCardCarousel`，见二十九节），传入 `showActions={false}` 关闭收藏/留言/分享按钮行

### 28.3 卡片集拖拽排序、编辑弹窗（`MindCardFolderEditSheet.tsx`）、删除确认（`ConfirmDialog.tsx`）——已抽成共享组件

原本写死在 `MindCardsProfileView.tsx` 内部、无法被详情页复用的编辑/删除逻辑，已抽成独立文件，供"看自己的卡片集"（`MindCardsProfileView`）和"卡片集详情页"（`FolderBrowseView`）共同复用，不重复实现。

### 28.4 详情页数据不再是快照，改为实时派生

`MindCardsProfileView.tsx` 里的 `browsingFolder` 状态，改成只存 `{id, isOwn}`，每次渲染都从 `folders`/`subscriptions` 列表里现查完整数据——**踩过的坑**：如果存一份完整快照，编辑保存之后列表更新了，但详情页显示的还是打开那一刻的旧数据，感觉像是"没保存"（其实后台真的存进去了，只是页面没跟着刷新）。同一个坑也出现在"我的卡片"详情弹窗（`detailCardId` 同理只存 id）、收藏按钮状态。

### 28.5 查看他人主页（`MindCardsUserProfileView.tsx`，新增，未接入任何页面）

只读版本的三栏（卡片/卡片集/订阅），去掉一切编辑/删除/新建能力，新增"订阅"按钮（复用 `mind_card_folder_subscriptions` 现成接口，本轮补上了文档里记录已久但一直没做 UI 的缺口）。这个组件**目前没有被任何页面引用**，等用户想清楚整体个人主页要挂载哪些模块、以及排布顺序，再决定怎么接入现有的 `u/[handle]/page.tsx`（这是一个已存在的、独立于片语的社交主页，含头像/关注按钮/发消息按钮，"点头像跳转到发布者主页"这个需求应该接进这个已有页面，不是新建页面）。

---

## 二十九、主浏览页轮播（`MindCardCarousel.tsx`）重构

### 29.1 卡片尺寸——JS 精确测量，不再靠 CSS 组合去猜

**教训**：flex + aspect-ratio 组合、以及"transform 位移动画的容器"内部去测量子元素尺寸，这两类 CSS 组合在这次对话里反复翻车（要么宽度算不对、要么在窄屏幕下比例跑偏、要么在特定容器结构下完全测不出数据）。最终方案：直接用 JS 测量真实可用空间，分别按"高度撑满、宽度按 3:4 换算"和"宽度撑满、高度按 3:4 换算"各算一次，取不超出可用空间的那一种——卡片本身任何情况下都严格 3:4，不依赖 CSS 的隐式行为。

### 29.2 两侧卡片——伪纵深感，不再是"裁切+精确算出露出百分比"

两侧卡片缩小到 `SIDE_SCALE=0.78`、透明度降到 `SIDE_OPACITY=0.55`、往下偏移 `SIDE_Y_OFFSET_PX=24`，模拟"退到后排"的透视错觉。不再靠"容器故意做得比卡片宽、露出固定百分比"这套算法——两侧卡片是完整、缩小显示的卡片，不存在"被裁掉一部分"的生硬边界，不需要渐隐遮罩柔化。

### 29.3 卡片进场动画——新卡片进场也要有滑入效果，不能瞬间冒出

每张卡片位置用"跟 `currentIndex` 的相对偏移量"计算（不是"prev/current/next 角色固定、内容互换"的写法），配合 `initial`（首次挂载起点，比正常停留位置更远、完全透明）和 `animate`（目标位置），刚好轮到、第一次出现在窗口里的卡片，也会"从更远处滑入+淡入"，跟已经在画面里、只是从别的 offset 移动过来的卡片，走的是同一套过渡节奏，不会出现"一个平滑滑动、一个瞬间闪现"的割裂感。

### 29.4 卡片整体缩放——`sizeScale` 可调 prop

不同调用场景（主浏览页 vs 卡片集"明信片"）天生能用的空间预算不一样大（主浏览页固定占着顶部栏+底部菜单栏，卡片集详情页是全屏独占）——组件新增可选的 `sizeScale` prop（默认 `0.7`），主浏览页单独传 `0.85` 弥补空间预算较小的先天差距，卡片集那边用默认值。

### 29.5 收藏/留言/分享按钮行——五格间距，跟详情弹窗对齐

改成跟详情弹窗同一套 `justify-around` 五格布局（两头用不可见占位补齐），保证浏览态和详情页的收藏/留言/分享落在同一个视觉节奏上，不是浏览态贴边分布、详情页居中分布两套不同逻辑。

### 29.6 键盘/滚轮翻页

新增：左右方向键、鼠标滚轮（横向或纵向都能触发）也能切换卡片，不再只有拖拽和点击。

---

## 三十、卡片详情弹窗（`MindCardDetailModal.tsx`）重构

### 30.1 按钮栏叠加进卡片内部，不再是卡片下方独立一条

`MindCardBody.tsx` 新增 `overlay` 插槽（一个普通的 `ReactNode` 子节点，最外层 div 固定给 `position:relative` 作为定位基准），浏览态小卡片和详情弹窗共用同一套叠加机制，不需要分别设计。

### 30.2 五格按钮：可见度/收藏/留言/分享/删除

可见度、删除只在自己的卡片上出现（`isOwn`），不是自己的卡片两头留空但位置照占着——收藏/留言/分享因此实际落在中间三格，浏览态复用同一套间距逻辑（见 29.5）。

### 30.3 `createPortal` 修复"祖先元素做了 CSS 变换导致弹窗错位"

**重要教训**：任何元素只要用了 `transform`（比如做位移动画），它下面所有 `position:fixed` 的子元素，就不再相对于整个屏幕定位，而是相对于这个"做了变换的元素"定位。明信片模式复用主浏览页轮播，轮播又可能待在"信息页/内容页横向轨道"这类做了 `transform` 的容器里，详情弹窗的"贴满整个屏幕"因此会跑偏、显示错位。**修法**：`MindCardDetailModal` 整体用 `createPortal` 直接渲染到 `document.body`，需要 `mounted` 状态守卫（SSR 阶段 `document` 不存在）。这个教训具有普遍性，任何"固定定位的弹窗，可能从做了 transform 的容器内部触发"的场景都要留意，见第三十六节。

### 30.4 动态尺寸公式——避免窄屏幕下比例跑偏

横板高度下限、竖版宽度下限，改成跟另一条边用同一条 `calc()` 公式实时联动（比如高度下限 `calc(min(90vw, 400px) * 4 / 3)`），不再是预先算好写死的数字——宽度在窄屏幕上会被 `min(90vw, 400px)` 压缩变小，如果配套的高度下限还是按 400px 这个理想值提前算好的固定值，两者会各自变化、互相脱节，比例跑偏。

---

## 三十一、命名体系重新定案

早期版本用的"收藏册/明信片/随笔册"这几个名字，本轮全部废弃，改用"英文选词精准、中文提炼到极简"的新方案：


| 类型        | 英文         | 中文     | `folder_kind`/`display_mode` 技术值                                       |
| --------- | ---------- | ------ | ---------------------------------------------------------------------- |
| 收藏册（网格）   | collection | **卡册** | `display_mode='album'`（不变）                                             |
| 明信片（轮播）   | deck       | **卡夹** | `display_mode='stack'`（不变）                                             |
| 感想（卡片+批语） | notebook   | **本**  | `folder_kind`：**需要从** `'journal'` **改成** `'notebook'`（见第三十二节，SQL 尚未执行） |
| 音频专属（未来）  | album      | **辑**  | 尚未设计，预留                                                                |
| 绘画专属（未来）  | gallery    | **廊**  | 尚未设计，预留                                                                |


**决策依据（避免以后重新纠结这几个词）**：

- `collection` 优于 `album`——`album` 让给音频场景（音乐行业里"专辑"是唯一、不可替代、没有歧义的词；收藏册这边换成 `collection` 语义依然清楚，损失的精准度更小）
- `deck` 保留使用——虽然在科技行业有"演示文稿"（pitch deck）的潜在歧义，但用户评估过这个项目不涉及幻灯片功能，风险可以接受
- `notebook` 优于 `journal`——`journal` 带"按天记录"的暗示（英语里跟 diary 距离很近），`notebook` 没有这层暗示，更贴合"想写就写、不限频率"的调性
- **其他 8 种语言的对应翻译，明确不在这次范围内**——这几个英文/中文名字只是"设计源头"，供项目内部讨论和命名统一使用，不代表其他语言必须有一个"完美对应"的词；各语言真正的界面文案，交给 next-intl 按各自语言的自然表达习惯翻译，不需要现在纠结"deck 在韩语里怎么说"

---

## 三十二、感想（notebook/本）——设计已定案，代码未实装

### 32.1 与卡册/卡夹的本质区别

不是"换个名字的收藏册"，是真正有独立功能的第三种类型：**左边是一张收藏进来的卡片，右边是用户自己写的一段批语**，一条一条往下排。卡册/卡夹单纯装卡片，没有"用户自己写的内容"这个维度。

### 32.2 待实装的改动

- 数据库：`mind_card_folder_items` 需要新增一个字段，专门存这段批语文字，只有 `folder_kind='notebook'` 类型的夹子会真正用到
- `folder_kind` 的 `'journal'` 这个技术值需要改名成 `'notebook'`（第三十节命名体系已定，SQL 待执行——虽然功能还没实装，用户决定趁早改好技术名，避免以后又要迁移数据）
- 返回卡片集列表的接口，目前完全没有把"这个夹子是卡册/卡夹/本三者中的哪一种"这个信息传给前端——这是本轮翻文档时发现的既有缺口，需要顺手补上
- "新建卡片集"表单里"随笔册（即将推出）"这个禁用占位选项，需要真正打开，文案同步改成"本"

---

## 三十三、留言系统——已完整实装

### 33.1 数据结构：两层（一级+二级），二级之间可以互相 `@` 引用

`mind_card_comments` 表：`parent_comment_id`（为空=一级留言；有值=二级留言，指向所属的一级留言，`on delete cascade`）+ `reply_to_comment_id`（只有二级留言之间互相引用时才有值，纯粹用于界面显示"回复 @谁"，`on delete set null`）。

**级联规则**：删一级留言，底下所有二级留言一并从数据库彻底删除（不做回收站）。删二级留言，只删这一条本身，引用它的其他留言不受影响——引用栏本身**永久保留**（"乙在回复某个人"这件事是既定事实），只是显示内容根据被引用的留言是否还存在，实时判断显示"具体内容摘要"还是"该评论已消失"这句占位提示。

### 33.2 权限

留言人本人、或者卡片作者，都能删除一条留言。能看到这张卡片的人就能留言，不设额外门槛。只能删，不支持编辑。

### 33.3 字符上限：150（按字形簇计数，不是按最原始存储单位）

- 上限定为 150（不是随便定的：TikTok 评论上限是 150，产品调性上片语跟 TikTok 更接近"轻量快速"而非 YouTube 那种鼓励长篇的调性；Instagram 是 2200、YouTube 是 10000，差异巨大，说明这类上限本身就该跟产品调性匹配，不存在业界统一答案）
- **计数方式必须用** `Intl.Segmenter`**（按"字形簇"计数），不能用浏览器原生** `maxlength` **或者 JS 的** `.length`——原生方案按最原始存储单位数，一个复合表情（比如"一家四口"）用这种方式数出来的长度可能是 8，不是 1；用这种方式截断，会把一张完整的表情图切成几个孤立的乱码符号。这不是我们自己发明的土办法，是业内已经公开验证过的正规做法（推特公开过的计数算法用的就是同样的思路）
- 工具函数 `countGraphemes`/`truncateToGraphemes`（`lib/mindCards/textLength.ts`）——**这是一个通用工具，不只服务于留言**，以后任何需要精确控制字数的地方（卡片内容本身、卡片集介绍）都应该复用这两个函数，不要重新发明
- **用户体验**：打字打满就不再生效，一次性粘贴/输入法一次性交上来的内容，能填满剩余空位的部分正常保留，填不下的部分直接消失，不做任何提示、也不会整个动作被拒绝掉——这套体验完全借用浏览器标准 `maxlength` 的做法（计数方式不借用，表现借用）
- **输入法组字处理**：组字过程中（拼音还没选字确认）不做任何截断，等组字真正结束（`onCompositionEnd`）才检查+按字形簇截断，否则会打断输入法正常工作
- 原生 `maxLength` 属性作为一道廉价的"粗防线"（放宽到精确上限的 8 倍，只用来挡极端粘贴场景，真正精确的判断始终交给 JS 层按字形簇计算的逻辑）
- 顶到上限时：输入框边框变红，正下方出现"字数上限150"这句静态提示（平时完全不显示任何字数提示，不是"还剩多少字"的倒计时）

### 33.4 留言面板（`MindCardCommentModal.tsx`）——跟卡片详情页同尺寸的独立面板，不是底部弹出的小抽屉

复用 `MindCardDetailModal` 的尺寸计算逻辑（横板/竖版分别对应卡片本身的方向），`createPortal` 到 `document.body`。**尺寸必须绝对固定，不能被内容撑大**——内部滚动处理超出内容，**踩过的坑**：横板尺寸设置误用了 `minHeight`（"下限"，内容多了照样会撑高整个面板）而不是 `height`（真正写死），导致面板被留言内容拉长变形。

### 33.5 留言展示排版（用户手绘打样定稿）

```
[作者名]                          ← 单独一行
[引用预览，仅二级且有引用时才有]      ← 紧接作者名下面
[留言正文]
[时间戳]  [回复]  [删除，仅本人]     ← 同一行，删除紧跟回复右边
[展开 N 条回复]                     ← 单独一行，独占最下面，仅一级留言且有回复时才有

```

- 删除：**纯文字链接**（不用垃圾桶图标、不用红色），跟"回复"视觉权重一致，避免暗示性过强；点击不直接删除，先弹确认（复用 `ConfirmDialog`）
- 回复输入框上方提示：`回复 {作者名}：{被回复内容预览}`，不是只显示名字
- "展开/收起 N 条回复"文案用"展开"，不是"查看"

### 33.6 二级留言排序规则

一级留言：时间**倒序**（最新的在最上面）。二级留言：时间**正序**（最早的回复在最上面）——回复要按发生顺序看才符合因果关系。

---

## 三十四、推荐算法——完整设计已定案，代码未实装

这是本轮讨论篇幅最长、也是最重要的一次方向调整，务必完整读完再动手，不要只看第七节那个旧公式。

### 34.1 核心方向转变：不是"混合评分"，是"四条并行独立的线，拼成一个推荐卡组"

**第七节那套"60%相似度+40%新鲜度"的混合公式，以及本轮讨论早期提出的"给行为共现一个默认很小的可调权重、混进同一个公式"这个方案，都已经被推翻，不要沿用。**

最终方案：一次推荐 10 张卡片，由四条**完全独立、互不干扰、各自平等**的机制分别选出，拼成一个卡组：

**同时具备八字和大五数据的用户（默认情况）**：

```
1,4,7 张 —— 八字相似度选出
2,5,8 张 —— 大五相似度选出
3,6,9 张 —— 行为共现选出
10   张 —— 随机选出

```

**只有八字、没有大五数据的用户（兜底方案）**：大五原本的名额，平分给八字和行为，随机不变：

```
1,2,4,7 张 —— 八字相似度选出（4张）
3,5,6,9 张 —— 行为共现选出（4张）
8,10   张 —— 随机选出（2张）

```

**为什么不能用混合评分**：用户自己的判断——"我怀疑我定的相似度算法，需要行为数据来辅助肃正"。如果把行为数据混进同一个公式变成一个分数，就没法干净地看出"单靠命理相似度、不受行为数据干扰"的推荐效果到底怎么样，两种判断方式的效果会搅在一起分不清谁起作用。要真正验证"命理相似度这套算法准不准"，必须让两套逻辑**分开独立跑、分开收集用户真实反馈、再对比**——这是经典的"控制变量"实验设计思路，不是"改进算法"这个单一目标，是"同时具备实验能力，让产品自己成为验证命理/心理学假设的工具"。

### 34.2 每张卡片必须标记"是哪条线选出来的"

新建一张记录表，字段包括：推荐给谁、这张卡片是谁、**是哪条线选出来的**。**这个字段必须是普通字符串，不能设成写死几个选项的严格枚举**——以后会陆续增加新的线（子维度相似度、星盘相似度、格局相似度……），不设枚举约束，加新线只是这个字段多一个新的文字值，不需要改表结构、不需要数据迁移。这是用户明确提出的要求："定义好接口，免得日后我又要修改数据库"。

### 34.3 可见度检查——见第三节铁律

四条线都必须走同一个"取卡片"入口函数，由这个函数统一做可见度过滤，不允许每条线各自调用检查。

### 34.4 "行为共现"研究工具——现在就做，不等数据攒够

**决策依据（用户的原话逻辑）**：

1. 现在数据量小、算出来的结果确实是噪音，但噪音本身不会让用户看到令人反感的内容（风险可控），比"以后交给没参与过这次讨论的工程师、他难以理解需求"这个风险更值得优先规避
2. 这不是一个"会自我学习、越用越准的模型"（不是贝叶斯优化那一类），是一段**每次现查现算**的统计逻辑——每次运行都是拿数据库里当下真实存在的收藏/关注/留言记录重新算一遍，本身没有"记性"。**真正在积累的是原始行为数据本身**（收藏表、关注表、留言表），不管这段统计代码写没写、用没用，这些数据都会随着产品正常使用持续积累。所以"现在写"和"结果现在没有参考价值"这两件事不矛盾——工具这次就做，结果好不好看是以后的事
3. **不会造成"越推越窄的信息茧房"**——因为它只是四条并行线里的一条，另外三条（尤其八字/大五相似度）一直在正常起作用，行为共现这条线的影响范围被架构本身死死限制住，不会一家独大
4. 需要留意但不必人为设限的风险：如果这条线推荐的内容，用户因此产生了新的收藏/关注行为，这些新产生的行为数据，下一次统计的时候会被当作"真实证据"用进去——这不是"算法在自我强化"（因为算法本身不积累状态），是"数据层面可能被推荐结果轻微影响"这个更隐蔽的现象（业内称"曝光偏差/推荐反馈回路"）。但因为影响范围被其他三条线死死限制住，这个风险的"天花板"是可控的，不需要额外的人工干预机制

### 34.5 数据记录原则——存客观事实，不存算法判断结果

**这是本轮讨论中最重要的一条通用原则，不只适用于推荐算法**：现在还不确定"命理相似度该怎么定义才准确"（用户举的例子：火命从财格 vs 水命从财格，五行能量分布差很远，但因为都是"从财格"这个格局，性格特质却很相似——说明现在"比五行数值距离"这套算法，很可能不是衡量"性格像不像"的正确方式，格局这类结构性分类可能才是真正关键，但格局判断算法现在还没做出来）。**在还不确定"什么才是正确的判断方式"这个前提下，记录数据的时候，绝不能把"当前这套可能有问题的算法"算出来的中间结果焗死存进历史记录里**——应该存能够回头重新计算的原始客观事实（比如互动发生时，双方各自是谁、互动的是哪张卡片、什么类型的互动、什么时候），不存"当时算出来的相似度分数"这类可能是错的加工结果。这样以后想通了更准确的判断方式，可以拿老数据重新算一遍，不会被困在"当初存错了公式算出来的东西，现在没法回头改"的坑里。

**具体到关注、收藏，不需要新建"事件流水表"**：`follows` 表、`mind_card_folder_items` 表已经天然记录了"谁、对谁、什么时候"这三要素（取关/取消收藏直接删行，不留痕，这是明确决定——"取消"这个动作对研究没有意义，不需要记录）。真正需要新建的只有"留言"这一张表（本身也是留言功能自己需要的字段，不是额外为了研究才加的）。**研究阶段直接查现有这几张表联查即可，不需要另起一张统一的"行为事件表"。**

### 34.6 施工顺序建议

1. 确认/补全大五因子是否已经真正生效（用真实数据测，不要只看代码猜）
2. "行为共现"统计工具（读现有表，产出一份"谁跟谁经常喜欢同一批内容"的简单列表，供研究用，这次先不接入正式推荐）
3. 四线并行的推荐接口本体（含"标记来源"字段的新表）
4. 前端展示（暂时不需要在界面上区分"这张卡是哪条线来的"，用户不需要知道，只有后台记录需要知道）

---

## 三十五、IP 地区标注功能——已完成法律研究，决定暂不实装

### 35.1 中国大陆——法定强制要求，已查证属实

网信办 2022 年发布的《互联网用户账号信息管理规定》，要求境内用户显示到省份，境外用户显示到国家，用户不能自行关闭。中国大陆版本以后接入时，这是**法定义务**，不是可选功能。

### 35.2 国际服务器——不违法，但有明确的实现约束，不要再重新研究一遍

- IP 地址本身在 GDPR 下算"个人信息"（欧洲法院 Breyer 案确认），但"算个人信息"不等于"不能显示"——GDPR 要求的是"有合法理由（legitimate interest 可以支撑这类低风险的场景）+ 在隐私政策里披露"，不是一刀切禁止
- **关键约束，两地监管殊途同归**：**只能显示"反查出来的国家"这个粗粒度结果，绝对不能显示原始 IP 地址本身**——原始 IP 理论上能追溯到具体某个人，而"这个人在法国"这种粗粒度信息，不足以单独定位到具体是谁，风险量级完全不同
- 结论：显示国家不违法，前提是隐私政策同步更新披露这件事，且技术实现上从不向前端暴露原始 IP

### 35.3 暂不实装的原因——成本问题，不是法律问题

需要"IP 反查国家"这个能力，项目现在没有，两个选项都要新增依赖：

- 方案 A：本地打包一份免费的 GeoIP 数据库（比如 MaxMind GeoLite2），需要定期手动更新
- 方案 B：调用第三方免费 GeoIP 查询接口，每次发留言/卡片多一次网络请求，速度稍慢，且要看免费额度够不够

用户评估后决定**这次不做**，成本相对功能收益不划算。**以后要做的话，直接按本节结论实现即可，不需要重新做法律研究**——只显示国家、不显示原始 IP、隐私政策同步更新，这三条已经想清楚了。

---

## 三十六、本轮踩过的技术坑汇总（供以后新会话/新工程师参考，避免重蹈覆辙）

- `width:'auto'` **不等于"按内容收缩"**：对普通 block 元素，真实含义是"撑满父容器能给的宽度"，要真正"按内容收缩/生长"必须显式声明 `width:'fit-content'`
- `aspect-ratio` **不会让内容超出时把盒子撑大**：它只划定一个固定比例的框，内容超出是溢出到框外面，盒子本身不变。要让盒子跟着内容真正生长，只能用 `height:'auto'`（普通盒子高度默认由内容撑开）+ `minHeight` 兜底
- `minHeight` **≠** `height`：`minHeight` 只是下限，内容多了盒子照样会被撑高。想要"绝对固定、内容多了内部滚动"，必须用 `height`（真正写死），这次留言面板被拉长变形，根源就是这个混淆
- **百分比高度对着"高度不确定"的父级（只有** `minHeight` **没有** `height`**）会静默失效，退回** `auto`——这类场景应该让父级变成 flex 容器，子层用 `flex:1` 撑满，或者干脆用 JS 测量后传入明确的像素值，不要依赖百分比高度这种在特定条件下会失效的机制
- **任何元素只要用了** `transform`**（比如位移动画），它下面所有** `position:fixed` **的子元素就不再相对于整个屏幕定位，而是相对于这个"做了变换的元素"定位**——这是详情弹窗在明信片模式下错位的根因，修法是 `createPortal` 到 `document.body`，跳出这层影响
- **原生 HTML** `maxlength` **按最原始存储单位计数，不是按用户直觉的"一个字符"**——复合表情符号（比如带 ZWJ 连接符的家庭表情）可能被数成 7~8 个单位，用这套方式做精确的字数上限判断和截断都不可靠，必须用 `Intl.Segmenter` 按"字形簇"处理
- **透过一个"正在做 CSS 变换/尚未完成布局"的元素去测量它子元素的实际尺寸，容易测不准**——应该找一个稳定、不参与动画的容器单独测量，测量结果作为明确数值往下传，不要依赖"透过动态元素间接测量"这种链路
- **对可能残缺的数据做兜底，要按字段各自兜底，不能只判断整个对象是否为 null/undefined**（`CLAUDE.md` 已有记录，本轮 `browsingFolder`/`detailCardId` 改成实时派生也是同一类教训的延伸：**存快照容易导致"数据其实更新了、但显示的还是旧的"这种"看起来像是没保存成功"的假象**，凡是"打开时拍一份快照"的写法，都要考虑改成"只存 id，实时从源头列表派生"）
- **收藏/订阅这类"点击后请求后端"的操作，必须等待真实响应确认成功后再让界面呈现"已生效"的样子**——不能点击后立刻乐观地改变界面状态、也不检查请求是否真的成功，失败时要悄悄把状态改回去（不点亮/不显示为已生效），不额外弹错误提示打扰用户
- **多个 chat 会话/多次施工并行时，务必先确认"发给我的文件内容"和"实际部署运行的文件内容"是同一份，再动手排查逻辑问题**——本轮至少两次报错的真正原因，都是文件实际内容与预期不符（内容被覆盖/被换成另一个文件的内容），而不是代码逻辑本身有问题，纯靠看代码逻辑排查不出这类问题，必须先怀疑"文件本身对不对"

---

## 三十七、第四轮施工完成情况（Claude Code + 本 Project 对话后续大量迭代）

第三十四～三十六节定下的设计，本轮已经全部实装并经过多轮实测修复，具体最终形态见第三十八～四十一节。

**Claude Code 完成的部分**：提醒系统全套（通知表统一、留言/回复通知区分、单卡片接口、通知列表+清扫）、推荐算法四线并行重写、"本"类型改名+批语字段+接口补全。施工中顺带清理了一个历史遗留的孤儿重复文件（`comments/replies/route.ts`，缺 `[id]` 动态段，从未被前端调用，导致 `next build` 报错）。

**本 Project 对话后续修复/深化的部分（体量不小，不是小修小补）**：

- 提醒面板尺寸/交互重做（见本节延续）
- 收藏通知的判定范围 bug 修复（"是否第一次收藏"从"是否第一次进我名下任意夹子"收紧为"是否第一次进这一个具体夹子"）
- 卡片集详情页架构整体重做了不止一轮（分页→无限滚动→清单+批量加载两层结构，见第三十八节）
- 留言系统追加乐观更新（见第三十九节）
- 卡片集名称/介绍加字数上限（见第四十节）
- 个人主页从"两个文件、不同网址"改成"业界标准做法：一个文件、同一网址、内部判断"（见第四十一节，这是最后也是最大的一次架构调整）

## 三十八、卡片集详情页最终架构（清单 + 批量按需加载，三种类型统一连续滚动）

这一块是本轮返工次数最多的部分，最终定型的架构如下，**以此节为准，不要参照更早的分页/纯滚动列表描述**。

### 38.1 两层数据结构：清单（manifest）+ 批量内容（batch）

- `GET /api/mind-cards/folders/:id/manifest`：一次性返回夹内**全部**卡片的轻量清单（`card_id`/`added_at`/`annotation`，不含 `content`/`style`），经过可见度过滤。查得快，翻页/滚动导航拿到这份清单就能立刻知道"总共有多少"，不被具体内容拖慢。
- `POST /api/mind-cards/batch`：传一批 `card_id`，返回这些卡片的完整内容（`content`/`style` 等），可见度**重新校验一遍**（不盲信清单当时的过滤结果——两次请求之间卡片可见度可能已经变化）。单次最多 100 个 id，超量客户端自动分批发送。通用接口，不专属于卡片集，以后任何"已知一批 id、需要补全内容"的场景都可以直接复用。
- 前端维护 `cardDataById: Map<id, MindCard>`，按需调用 `ensureLoaded(ids)` 补内容，已有的/正在加载的自动跳过，不重复请求。

**踩过的坑**：早期版本把"测量卡片尺寸用的 ref"挂在了"要等尺寸算出来才会渲染"的元素上，形成死循环（没渲染出来就量不到，量不到就永远算不出尺寸），导致卡夹内容区完全空白。修法是把这个 ref 固定挂在最外层、**无条件渲染**的滚动容器上，不能挂在被自己要计算的结果所"保护"的元素上。

### 38.2 三种类型统一成一条连续纵向滚动，不再分页、不再有独立信息页

- 信息区（标题/简介/可见度）占满一页高度，紧接着下面就是内容，**没有"点箭头才能进入内容"这一步**，往下滚动自然过渡
- 信息区内部布局：标题/简介固定贴顶一段距离、可见度固定贴底一段距离（**用像素值，不用百分比**——百分比定位没有"绝对不会被突破"的安全距离，简介短或者页面矮的时候容易跟下面内容重叠，这是实际踩过的坑），中间用弹性空白撑开
- 卡册（网格两列）：不再分页，往下无限滚动，滚到底部（`IntersectionObserver` 哨兵）触发加载下一批。卡片尺寸：优先让高度刚好装下两排（含排间距），装不下再退回按宽度算。网格容器宽度=两张卡片+间距的实际宽度并居中，**不能撑满整行**（撑满会导致卡片贴左、右边一片空白——早期版本踩过这个坑）
- 感想（本）：横 4 竖 3 的画框，细灰线边框、**无背景色**；框高+容器上下留白=一页；卡片贴内边距按 3:4 摆放，批语占剩余宽度，**左对齐+整体垂直居中**（不是文字本身居中对齐，是文字块作为一个整体在可用空间里居中），字号固定 18px（这是估算值，卡片系统本身如果有确切的五档字号像素映射表，应该改成直接引用那份表，这次没有拿到，先用具体数字顶上）。批语支持编辑（右上角铅笔图标+弹窗，复用"发布收藏时同一套弹窗写、点保存"的交互习惯，不做原地变成输入框那种——画框是精确算出来的固定尺寸，原地编辑容易撑破）
- 卡夹（明信片）：**接入独立开发的纵向轮播组件**（`MindCardVerticalCarousel.tsx`），不是普通滚动列表——保留了横向轮播那套"伪纵深感"（上下相邻卡片缩小+变淡+错位）+ 拖拽吸附 + 键盘/滚轮控制，只是坐标轴从横向换成纵向。卡片尺寸复用横向轮播同一个比例（高度=可用高度×0.7）。**接入连续滚动时，鼠标在卡片范围内滚轮只切卡片、不触发页面滚动**（滚轮监听改成 `{passive:false}` + `preventDefault()`，被动监听拦不住默认滚动，会导致"切卡片的同时页面也跟着抖一下"）；鼠标在卡片范围外滚轮正常滚页面——这是用户自己想出的方案，用鼠标位置做区分，比之前设计的"点箭头切换、避免手势冲突"更优雅，替代了原方案。
  - **卡夹内容区要额外预留"伪纵深卡片探出去的空间"**：上下相邻卡片是靠绝对定位主动探出当前卡片本身的边界之外实现的效果，这部分空间如果不额外预留，会顶进邻近区域（信息区的可见度文字被探出来的卡片盖住，就是这个原因），预留量是当前卡片高度的 0.5 倍（经验值，非精确计算）

### 38.3 层级统一，不再手动排大小

早期用 `z-40`/`z-[60]` 这类手动比大小的方式解决"卡片集详情页会不会被提醒面板挡住"，改成**所有全屏弹层统一用同一个** `z-50`，靠"后挂载的自然盖在先挂载的上面"（DOM 顺序 + 相同 z-index 的浏览器默认堆叠规则）解决，不需要为每个新弹层重新判断该给多大的数字、会不会跟别处冲突。

## 三十九、留言系统追加乐观更新

第三十三节已实装的基础上，追加：

- **发留言/删留言都改成乐观更新**：立刻在界面上生效（造一个临时 id 的乐观条目/立刻从列表拿掉），后台异步真正提交；成功了悄悄把临时 id 换成真实 id（用户无感知），失败了撤销刚才的乐观改动、提示"留言/删除失败"（几秒后自动消失，不需要手动关闭）
- **删除必须是"发请求之前就先移除"，不是"等确认成功才移除"**——早期版本是后者，导致删除到真正从界面消失之间有一段空档，用户看着"好像没生效"会忍不住点第二次，第二次点击目标已经不存在，报 404。改成乐观删除后，这个问题从根上消失
- 发送按钮从文字改成 `Send` 图标（省一处需要翻译成 9 种语言的文字）
- 新增 `GET /api/mind-cards/me`：返回当前登录用户自己的 `id`/`handle`/`display_name`，乐观显示"刚发的这条留言"时立刻需要知道作者名字该显示什么，不能等后端确认。这是个通用小接口，不专属于留言，后续"个人主页"架构（第四十一节）里"自己进个人页要先查自己的 handle"也复用了它

## 四十、卡片集名称/介绍字数上限

- 名称上限 **60 字符**，介绍上限 **120 字符**（暂定值，用户明确说了"以后做手机端再调整"，不是精确算出来的终值）
- 推算依据：按最"占地方"的支持语言（德语）估算，保证信息区固定高度的空间无论如何都装得下、不会被撑破；用真实德语句子实测过 120 字符能装下一句完整、有内容的句子（"Eine Sammlung meiner Gedanken über Liebe, Verlust und die Suche nach mir selbst." 正好 120 字符）
- 计数/截断方式复用留言那套（`countGraphemes`/`truncateToGraphemes`，按字形簇处理，不是简单字符串长度）
- **已知简化，不是遗漏**：这两个表单的字数截断，没有像留言那样额外处理"输入法组字过程中不要打断"的细节——这两个是相对次要的表单场景，属于可接受的简化，如果实测后发现确实影响体验，照留言那套逻辑补上即可

## 四十一、个人主页架构——最终定为"业界标准做法"，推翻了本轮中途定过的另一版方案

这是本轮篇幅最大、也是最后一次的架构调整，务必完整读完。

### 41.1 结论：一个文件、同一个网址，内部判断"是不是自己"

**本轮中途曾经定过"同一网址、不同文件"的方案（网址层面就分流到"看自己"和"看别人"两份独立文件），后来查证了 X（推特）等主流社交产品的通用做法后，用户主动推翻了这版方案，改成业界标准做法：只有一份文件、一个组件，内部判断"当前登录的这个人，是不是这个主页的主人"，用这个判断结果控制要不要显示编辑/删除/新建这些能力**。

**推翻的原因**：用户原本想要"两份文件"是为了"以后方便往不同方向做差异化"，但后来意识到"目前更新频繁，还没想清楚要做多大的区别化，一份文件能减少许多维护成本"——业界标准做法的核心优势就是"看自己"和"看别人"共享同一套渲染逻辑，不容易出现"两边各自维护、其中一边改了另一边忘了同步"这种不一致风险，这次以维护成本优先，选择了更经典、更保守的方案。

**如果以后真的需要拉开差距**：不需要现在就预留分叉点，届时可以在这同一份文件内部，针对 `isOwn` 这个条件，逐步长出更多分支判断——这是业界标准做法本身能自然支撑的演化路径，不需要为此重新设计。

### 41.2 网址与身份判断

- 网址：`/{locale}/dashboard/mind-cards/profile/{handle}`，**唯一路由**，不管看自己还是看别人都走这一条，不再有不带 handle 的旧路径（`/dashboard/mind-cards/profile/page.tsx` 这个旧文件已废弃，需要手动删除）
- 组件 `MindCardsProfileView.tsx` 接收 `targetHandle` prop，内部：
  1. 用 `GET /api/users/{handle}` 解析出目标用户（这是认证/社交模块本来就有的现成接口，不是新建的）
  2. 用 `GET /api/mind-cards/me` 查出当前登录用户自己是谁
  3. 两者的 `id` 比对，得出 `isOwn`
- 片语主页左下角"进个人页"按钮：改成先调 `/api/mind-cards/me` 查出自己的 `handle`，再跳转到 `/dashboard/mind-cards/profile/{自己的handle}`——不再有一个"没有 handle 的特殊入口"

### 41.3 页面内容差异（全部是同一份文件内部的条件渲染，不是分文件）

- 头像：**预留位置，不放任何占位图形**（不画圆圈、不放通用小人图标），纯粹在布局上占住这块空间，等以后真做头像功能，直接往这个位置填内容，不需要重新排版。这是设计需求，不是"忘了做"
- 名字 + `@handle`：不管看自己还是看别人都显示，保持两种情况下的版式一致
- 关注/私信按钮：只在 `!isOwn` 时出现，位置夹在 handle 和下方三栏切换之间
- 三栏文案："我的卡片"/"卡片"这一个差异点——`isOwn` 时叫"我的卡片"，看别人时叫"卡片"，**明确不用"他的卡片/她的卡片"这类带性别指向的说法**（用户原话："性别问题太敏感了"），卡片集/订阅这两栏文案两边一致，不受这条影响
- 编辑/删除/新建卡片集、改卡片可见度、删卡片——只在 `isOwn` 时可操作；看别人的卡片集只有"订阅"按钮

### 41.4 卡片数据补齐了作者信息（这次一并解决的既有缺口）

`recommend`/`following`/`profile/mine`/`batch` 四个返回卡片列表的接口，全部补上了：

- `author: {id, handle, display_name} | null`——**卡片本身的界面上只显示名字，不显示 handle**（用户明确说"信息太冗余"），但 handle 数据仍然要跟着传，因为"点名字跳转到作者主页"和"判断关注胶囊要不要显示"这两个动作背后都需要用到 handle
- `authorFollowedByViewer: boolean`——当前登录用户是否已经关注这张卡片的作者，决定卡片详情弹窗里"+关注"胶囊要不要显示（已关注/是自己的卡片，都不显示）

新增共享工具函数 `lib/mindCards/authors.ts` 的 `fetchAuthorMap`，这四个接口统一复用，不再各自重复实现一遍（留言、通知接口之前各自写过一份几乎一样的逻辑，这次收拢）。

### 41.5 卡片详情弹窗新增作者信息行

`MindCardDetailModal.tsx` 新增一行（叠在原有按钮栏正上方，两条独立，不挤在同一行）：头像预留空间 + 名字（点击跳转作者主页）+ "+关注"胶囊（乐观更新，失败悄悄撤销，不弹错误提示，遵循本轮对话反复确认过的收藏/关注类操作统一体验规则）。

**踩过的坑（已修复）**：详情弹窗组件在轮播场景下可能不会真正卸载重建，只是 `card` 这个 prop 换了内容——"是否已关注"这个本地状态如果只用 `useState` 的初始值，不会跟着 prop 变化自动同步，容易出现"换了一张卡片，关注状态还残留着上一张卡片作者的状态"这种串台问题。修法是额外用一个 `useEffect`，监听 `card.id` 变化时显式重新同步这个状态。

### 41.6 待验证/待清理

- `MindCardsUserProfileView.tsx`（旧的"看别人"独立组件）已被合并进 `MindCardsProfileView.tsx`，**需要手动删除**，不再使用
- 旧的不带 `[handle]` 的 `profile/page.tsx` **需要手动删除**
- 关注/私信按钮复用的 `POST /api/follows`、`GET /api/follows/status`、`POST /api/conversations` 这几个接口，是本轮对话更早以前审查 `u/[handle]/page.tsx` 时确认过存在、但**这次没有重新逐行核实内容**，如果这几个接口的实际签名/返回结构跟当时看到的版本有出入，这块可能需要联调修正
- 整个"合并个人主页"这块，后续在 TanStack Query 改造那一轮做代码审查时，逐文件核对过关键逻辑（`follows`/`conversations`接口签名、关注状态本地state同步等），确认没有问题，第三十九节的这条疑虑已解除

## 四十二、片语模块的 TanStack Query 改造

`Mindo-片语.md`（本文档）之前记录的所有手写`fetch`+`useState`逻辑，本轮已全部改造为使用TanStack Query（`useQuery`/`useMutation`/`useInfiniteQuery`）。改造范围覆盖片语几乎全部组件（主浏览页/详情弹窗/留言面板/收藏窗口/卡片集详情页/个人主页/通知面板/发布页），具体文件清单见git提交记录，不在本文档逐一重复列出。

### 44.1 改造原则："外壳不变，内脏可以换"

用户明确决定：不要求原样1:1替换，允许顺手做TanStack Query官方机制自带的体验优化（共享缓存/预加载/无限滚动官方能力）——但用户能感受到的行为（点击后多久有反应、翻页方式是自动还是要点按钮）不能变。已改用官方乐观更新写法（`onMutate`/`onError`/`onSettled`）重写了留言的发/删、卡片的收藏/关注/可见度修改这些原本手写的乐观更新逻辑。

### 44.2 值得记录的实现巧思

- **留言数/关注数这类"多处显示同一个数字"的场景，改成多个组件各自订阅同一个**`queryKey`**，不再靠回调层层传递**——比如卡片详情弹窗和轮播卡片上的留言数徽标，都直接订阅留言列表那份缓存的`totalCount`，留言面板内部发/删留言的乐观更新会自动反映到这些徽标上，不需要`onCountChange`这类回调
- **卡片集详情页（**`FolderBrowseView.tsx`**）改用**`useInfiniteQuery`**重写了"清单+按需批量加载"这套两层结构**，同时新增了`revealedCount`这个独立概念，专门解决"`useInfiniteQuery`本身要等真正拿到数据才更新状态，但产品体验要求骨架占位图立刻出现、不用等网络"这个矛盾——`revealedCount`只负责"现在该显示到第几条骨架"，具体每条骨架背后的真实内容什么时候到位，交给`useInfiniteQuery`在背后处理，两者互不干扰

### 44.3 改造过程中发现并修复的两个真实bug（不是配置问题，值得记录避免以后重犯）

**一、推荐流无限加载死循环**：判断"还有没有更多"的逻辑，早期版本写成"这次接口返回的卡片数量>0就认为还有更多"——但推荐算法每次都是独立重新选一批，不知道之前已经拉过什么，完全可能连续几次返回的都是已经见过的重复卡片（页面会把重复的过滤掉不显示）。"接口有没有返回东西"和"过滤重复后是否真的有新增"是两件事，只看前者会陷入"接口一直有返回、但实际新增趋近于零"的死循环，导致请求不停发出去。**修法**：判断标准改成"这次返回的里面，有没有哪怕一条是之前所有页里都没出现过的"，`getNextPageParam`里用`allPages`（排除最后一页自己）算出"之前见过的id集合"，跟最后一页比对。这条教训已同步记入`CLAUDE.md`"关键教训"（因为它不是片语专属问题，是任何"数据源没有天然游标概念"的场景都可能踩的坑）。

**二、**`useMemo`**依赖数组长度变化报错**：`MindCardCommentModal.tsx`里两处`useMemo`，依赖数组用`[...repliesResults.map(...)]`这种写法展开了一个长度随"展开了几条留言"变化的数组，触发React的"依赖数组大小必须每次渲染保持一致"这条规则报错。**修法**：这两处运算本身很轻，直接去掉`useMemo`，每次渲染都重新算一遍。这条教训同样已同步记入`CLAUDE.md`。

## 四十三、翻译功能——方向已从"Google Cloud Translation"调整为待定，原因是绑卡风险

第二十七节此前记录的方向（用Google Cloud Translation API）**已被推翻**，原因是查证后发现：即便只想用免费额度，也必须先绑定银行卡开通付费账户；超出免费额度后是**自动按超出部分扣费**，不是自动停止服务；Google提供的"预算提醒"默认只是发通知，不会自动切断服务，除非额外自己搭一套自动化机制去响应这个提醒。用户明确表示这种"可能在不知情的情况下被扣钱"的风险不可接受。

**候选方案：DeepL**——免费额度不需要绑卡，超出后是直接停止服务（不会自动扣费），这个特性直接命中用户的顾虑。语言覆盖：英/法/德/西/意/日/韩/简体中文确认支持；**繁体中文（Mindo九种语言之一）存在一个未完全确认的疑点**——查到的资料显示DeepL的"文档翻译"功能支持繁体中文，但"文字翻译接口"（Mindo实际需要的这种）是否同样完整支持繁体中文，没有查到能完全确认的资料，需要在正式对接前，直接去DeepL官方文档（`developers.deepl.com/docs/getting-started/supported-languages`）核实。免费额度每月5万字符（远低于Google的50万），换来的是不用担心意外扣费。

具体接入方式、失败降级显示等细节，待用户确认DeepL繁体中文这一点没问题后再展开讨论，本节先记录方向调整本身。

**结论**：确认要引入，但不是现在——用户明确决定"先把片语做完（含实测），再装这个工具"，且明确表示**不需要跟"下一个具体做什么新模块"绑定**，装的时机只取决于"片语这边告一段落"这一个条件，跟以后做论坛还是私信无关。

**为什么考虑引入**：这次对话里"乐观更新+失败回滚"这套模式，在收藏、留言、关注、卡片集等好几个地方被反复手写实现，论坛、私信这两个已经确定要做的未来模块，几乎必然还会重复同样的模式（私信尤其是"乐观更新"最经典的应用场景）。TanStack Query（原名 React Query，同一个团队做的同一个东西，改了个更通用的名字）是这类需求业内最主流的解决方案，每周下载量千万级，横跨多个行业被广泛采用，不是小众有风险的选择。

**成本收益判断**：

- 学习这套工具具体怎么用，成本几乎全部落在开发这边（不需要用户学任何东西），但需要专门投入时间搞懂它的概念和写法
- **不会跟数据库产生任何耦合**——这个工具只管"网页这一端怎么记住/怎么重新问一遍网络请求拿到的数据"，跟数据库表结构、后端接口设计完全无关，用户曾经担心"以后数据库会不会因为这个工具变得没法改"，这个顾虑经过讨论确认不成立
- 现在就把片语这批已经跑通的代码全部推倒重做，成本很高（十几个文件、几十处发请求的地方，且是已经反复调好、有风险的现成代码），性价比不划算
- **正确的引入时机是：装好工具后，从下一个全新模块（不管是论坛、私信、还是片语以后新增的功能）开始直接用这套工具写，片语现有的代码先不动**，等以后有专门的时间、对这套工具已经足够熟悉，再回头做一次性的迁移

